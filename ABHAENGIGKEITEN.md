<!-- ROLLE: abhaengigkeits-register -->
# 🔗 Abhängigkeits-Register

**Zweck (Adam-Auftrag 16.07.2026):** Schutz gegen stille Abhängigkeits-Brüche —
das „**Excel-`#BEZUG!`-Problem**": Eine Komponente wird geändert oder entfernt,
und irgendwo bricht unbemerkt etwas, das davon abhing.

**Verbindliche Regel (steht auch in CLAUDE.md):**
1. **Vor** jeder Änderung/Entfernung einer Komponente: hier nachsehen, wer davon abhängt.
2. **Danach** die abhängigen Komponenten mittesten (Prüfbefehl aus der Tabelle).
3. **Beim Bau neuer Features** deren Bezüge **sofort** hier eintragen — nicht später.

**Verkabelung:** Der tägliche 4-Uhr-Funktionscheck (**8.1**) arbeitet die
Prüfbefehle dieser Tabelle ab und meldet Brüche proaktiv per Telegram. Der
Regressionstest (**8.2**) nutzt sie als Prüfliste nach Änderungen.

> Lesart der Tabelle: **Komponente** → wer sie braucht → womit man prüft, dass sie trägt.
> Prüfbefehle laufen (soweit nicht anders vermerkt) **auf dem VPS**; `[Mac]` = am Mac.

---

## Laufzeit-Dienste

| Komponente | Wird benötigt von | Prüfbefehl / Prüfkriterium |
|---|---|---|
| **systemd `claude-telegram-bot`** | Alles Nutzerseitige (Telegram-Betrieb 24/7) | `systemctl is-active claude-telegram-bot` = `active` |
| **systemd `searxng`** (127.0.0.1:8888) | Bot-Websuche `web_search` (2.7) → damit die gesamte Fakten-/Quellenprüfung (Antwortqualität) | `systemctl is-active searxng` **und** `curl -s "http://127.0.0.1:8888/search?q=test&format=json" \| grep -q '"results"'` |
| **systemd `ollama`** + Modell `phi4-mini` | LiteLLM-Route `local` → Neben-Inferenzen (2.6), künftig **Ampel-Enforcement** (rot → lokal) | `systemctl is-active ollama` **und** `ollama list \| grep -q phi4-mini` |
| **systemd `litellm`** (127.0.0.1:4000) | `_litellm_complete()` → `_ai_topic_label` (TTS-Kapitel), künftige Neben-Inferenzen | `systemctl is-active litellm` **und** `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:4000/health/liveliness` = `200` |
| **launchd `com.jakuna.vps-backup`** `[Mac]` | Tägliches Backup (4.1) — einzige Sicherung der nur-lokalen VPS-Daten | `launchctl list \| grep -c vps-backup` = `1` |
| **systemd-Härtung** `claude-telegram-bot.service.d/hardening.conf` (23.07.: NoNewPrivileges, ProtectSystem=strict, ReadWritePaths=/home/claudebot, PrivateTmp) | Schadensradius-Begrenzung des Agenten (Rotes-Team C.2). ⚠️ Der Bot/Agent kann seither NUR unter `/home/claudebot` + eigenem /tmp schreiben — neue Features, die anderswo schreiben wollen, brechen still; dann ReadWritePaths erweitern (bewusster Root-Eingriff), nicht die Härtung entfernen | `[VPS]` `systemctl status claude-telegram-bot` zeigt Drop-In `hardening.conf`; Funktionsprobe = Selbstcheck grün nach Neustart |
| **Hygiene-Neustart** `claude-bot-hygiene.timer` (04:00) + `[STILL]`-Marker in `bot.py` | Prozess-Hygiene gegen MCP-/Session-Degradation (Rotes-Team C.3). ⚠️ Kette: Timer schreibt `[STILL]`-Restart-Reason → bot.py unterdrückt bei sauberem Start die Telegram-Meldung; wird der Marker-String umbenannt, meldet der Bot wieder jede Nacht um vier | `[VPS]` `systemctl list-timers claude-bot-hygiene.timer`; Log am Morgen: „Hygiene-Neustart sauber — Startmeldung unterdrückt" |
| **Fundament-Pinning** `requirements.txt` (`claude-agent-sdk==0.2.127` bündelt CLI 2.1.219) | Schutz vor stillen Breaking Changes (Rotes-Team B.1). ⚠️ SDK-Update NUR im Wartungsfenster nach `scripts/regressionstest.sh`; die globale npm-CLI (root-eigen) ist getrennt. **⚠️ Pin-Sync-Pflicht:** Nach jedem via `/updates` bewusst übernommenen gelben (gepinnten) Update MUSS die Migrations-Sitzung den Pin hier nachziehen — der Bot kann `requirements.txt` nicht schreiben (8.7), sonst fällt ein Rebuild auf die alte Version zurück | `grep '==' requirements.txt`; `.venv/.../claude_agent_sdk/_bundled/claude --version` = gepinnter Stand (aktuell 2.1.219) |
| **Gestalten-Erzeuger** `scripts/mailgestalten.py` (ROLLE `mailgestalten`) | Rang 0 des Mail-Umbaus: erzeugt die Mail-Gestalten aus Norm-Mengen der Standardbibliothek statt aus Handauswahl und misst `mailtext.lesbar()` gegen den **rohen MIME-Rumpf**. Haengt an `mailtext._STUMM`, `_UNSICHTBAR_STIL` und an `mailtext.lesbar()`. **BEWUSST NICHT in `regressionstest.sh`**, bis die Reparatur durch ist — er ist ab Tag eins rot und blockierte sonst jeden Commit (Engywucks Auflage 1) | `.venv/bin/python scripts/mailgestalten.py --deckel 240` → Achsenstand + Tabelle nach Aufbau/Kodierung; **Zeile d (Gegenrichtung) MUSS gruen sein**, alles andere ist derzeit erwartbar rot |
| **Websuche-Ausfallmeldung** `bot.suchlage` / `bot._websuche_gesamt` | Trennt [gar nicht gesucht] von [nichts gefunden]. Haengt am Feld `unresponsive_engines` der SearxNG-Antwort und an `/config` fuer die Zahl der aktiven Zulieferer (Kategorie `general`) — **keine Namensliste**, damit sie nicht altert, wenn jemand die Konfiguration aendert | `.venv/bin/python scripts/test_websuche_ausfall.py` → 7/7; laeuft im Regressionslauf mit |
| **Websuche-Waechter** `scripts/websuche_check.py` (ROLLE `websuche-check`) | Taegliche Probe im 4-Uhr-Lauf (`daily_check.sh` 9h). Haengt an `bot.suchlage` und am lokalen SearxNG; fuehrt einen Verlauf unter `~/.claude/websuche-verlauf.json` fuer die Zwei-Tage-Daempfung. **Modellfrei** — zeitgesteuerte Laeufe duerfen keinen Modell-Aufruf ausloesen | `.venv/bin/python scripts/test_websuche_check.py` → 7/7; live: `websuche_check.py` auf dem VPS |
| **Freigabedialog-Klartext** `bot.format_tool_call` / `bot.einstufung` / `bot.kontext_angaben` | Adams Entscheidungsgrundlage im Vier-Augen-Prinzip. Haengt am `ToolPermissionContext` des SDK (Felder `decision_reason`, `blocked_path`, `title`, `display_name`) und an `tool_input[description]`. **Herkunft ist getrennt:** Sitzungs-Angabe = behauptet, CLI-Felder = gemessen. Ein unbekanntes Werkzeug meldet sich ausdruecklich, statt still durchzufallen | `.venv/bin/python scripts/test_freigabedialog.py` → 7/7; laeuft im Regressionslauf mit |
| **Stundenblume: Melden nach Aenderung** `stundenblume._daempfen` / `_lage_ausgeben` | Adams Meldeverhalten. Kennungs-Praefixe steuern die Zustellung: `p:` nur ins Protokoll, `!` ohne Entprellung (Ereignisse wie die Kettenluecke). Entwarnte Kennungen bleiben im Gedaechtnis, sonst greift die Sperrfrist nicht. `daily_check.sh` 9i ruft `--lage` und liest **dieselbe** Datei wie der Daempfer — kein zweiter Zustand | `.venv/bin/python scripts/test_stundenblumen.py`; live: `stundenblume.py --lage` |
| **8.2-Minimaltest** `scripts/regressionstest.sh` | Wartungsfenster-Freigabe (Fundament-Updates), Regressionsnetz nach Änderungen | `bash scripts/regressionstest.sh` → 11/11 (Mac UND VPS) |

## Zugänge & Geheimnisse

| Komponente | Wird benötigt von | Prüfbefehl / Prüfkriterium |
|---|---|---|
| **`CLAUDE_CODE_OAUTH_TOKEN`** (in `/etc/claude-telegram-bot.env`) | **Bot-Agent** (jede Session). **Ausnahme:** LiteLLM/Neben-Inferenzen brauchen ihn NICHT (laufen lokal über Ollama — F1-Leitplanke). 💰 Nie durch `ANTHROPIC_API_KEY` ersetzen! | Token gesetzt **und** Mini-Inferenz: als `claudebot` mit Token → `claude -p "1+1="` liefert `2`. **⏰ Läuft ~14.07.2027 ab** (Sidecar `/etc/claude-telegram-bot.token-issued`, Frühwarner = 5.20) |
| **SSH-Key `~/.ssh/id_ed25519` + Aliase `claudevps`/`claudebot`** `[Mac]` | Backup (4.1), Code-Deploy (`git pull`), gesamte Fernwartung dieser Sitzung | `ssh -o BatchMode=yes claudevps true` (Exit 0) |
| **GitHub-Deploy-Key** (`/home/claudebot/.ssh/github_deploy`, read-only) | Code-Deploy auf den VPS (`git pull origin mac-produktivstand`) | `sudo -u claudebot ssh -n -o BatchMode=yes -T git@github.com 2>&1 \| grep -q "successfully authenticated"` — **`-n` ist Pflicht!** Ohne `-n` frisst das innere `ssh` die Standardeingabe des umgebenden Skripts, alle Folge-Prüfungen fallen still aus (am 16.07. genau so passiert) |
| **Telegram-Bot-Token + `ALLOWED_USER_IDS`** | Bot-Erreichbarkeit + Zugangsschutz | Bot-Log zeigt `Application started` ohne `401`/`Conflict` |

## Daten (nur lokal — nicht in Git!)

| Komponente | Wird benötigt von | Prüfbefehl / Prüfkriterium |
|---|---|---|
| **Ampel-Regeldateien** `~/.claude/ampel_rules.toml` + `ampel_custom.json` | Ampel-Klassifizierung (2.2), `/ampel`-Menü + Textbefehle, künftiges **Enforcement**. Enthält Klienten-Namen → **nur lokal, nie Cloud/Git** | `ampel.status()["rules_file_exists"]` = `True`; `/ampel regeln` listet Regeln. **Fehlt die Datei → stille Rückstufung auf eingebaute Defaults (Klienten-Regeln weg!)** |
| **Bot-Gedächtnis** `~/.claude/memory/` (+ `MEMORY.md`-Index) | Session-Kontext (5.23), Selbstcheck, Identität/Verhaltensregeln. **Index-Zeile fehlt → Datei wird still NICHT geladen** | `load_user_memory()` liefert nicht-leer; `MEMORY.md` vorhanden; Selbstcheck ohne „MEMORY.md fehlt" |
| **Whisper-Modelle** `models/ggml-medium.bin` **und** `ggml-small.bin` | Voice-Transkription; **beide** nötig für den STT-Umschalter (5.22) — fehlt eines, **verschwindet die 🎙️-Button-Zeile still** (`_discover_stt_models`) | beide Dateien vorhanden; `bot._discover_stt_models()` enthält `small` **und** `medium` |
| **Nur-lokale VPS-Dateien gesamt** (env, token-issued, memory, ampel, logs, configs) | **Backup 4.1** — es gibt keine zweite Kopie (nicht in Git) | `[Mac]` `bash scripts/vps_backup.sh --dry-run` listet alle Gruppen; `~/VPS-Backup/latest` > 0 B |
| **Restore-Fähigkeit des Backups** (nicht nur die Sicherung!) | Der gesamte Notfall-Plan — ein Backup, aus dem nie zurückgespielt wurde, ist ein **unbewiesenes Versprechen** (Adam 17.07.) | `[Mac]` Restore-Probe in ein **Testverzeichnis** (nie über die Originale): `ampel_rules.toml` per `tomllib` parsebar, `MEMORY.md` vorhanden + die darin verlinkten Dateien auflösbar. **✅ verifiziert 17.07.** (71/71 Links auflösbar, TOML ok, dokumentiert in 4.1). Wiederholbar als 4-Uhr-Check (8.1). `._`-AppleDouble-Ballast **bereinigt 17.07.**: `--exclude='._*'`+`.DS_Store` im Backup-Skript, Mac-Kopie + VPS-Quelle geleert; Herkunft = tar-Migration 14.07. (Linux-Bot erzeugt keine neuen) |
| **Chat-Logs** `logs/conversations/` (inkl. **Tageswechsel-Behandlung** im `ConversationLogger`, Fix 22.07.) | Session-Recall (`_recent_conversation_recall`), `_detect_pending_item` (liest gestern+heute), Backup, **4.2 zentrale Chat-Logs** (jede Tagesauswertung setzt voraus, dass Einträge in der Datei ihres Tages stehen — der eingefrorene Startpfad hätte sie verfälscht), künftig Recall-Index (5.11) | `[Mac]` `scripts/test_conversation_log_rollover.py` (simulierte Mitternacht: neue Datei + Kopfzeile, Verweiszeile in der alten, volle Datums-Stempel); live: Tagesdatei des heutigen Datums wächst. ⚠️ Turn-Köpfe tragen seit 22.07. das volle Datum — Parser müssen bei `## Du ·`-Präfixen bleiben (startswith), nie auf das alte `HH:MM:SS`-Format matchen |

## Steuerung & Schutzschichten

| Komponente | Wird benötigt von | Prüfbefehl / Prüfkriterium |
|---|---|---|
| **`.claude/settings.json` + `.claude/hooks/*.sh`** `[Mac]` | SessionStart-Banner + **Schreibschutz** auf MIGRATION.md/CLAUDE.md (Führungs-Register). Fehlt/unausführbar → Schutz still weg | Hooks ausführbar (`-x`); Blocktest: veralteter Klon → `guard-master-files.sh` Exit `2` |
| **Rollen-Marker `<!-- ROLLE: … -->`** in den 6 Schlüsseldokumenten + **`WIEDERANLAUF.md`** (22.07.) | Wiedereinsetzung neuer Kontroll-/Planungssitzungen (WIEDERANLAUF referenziert über Rollen); „Struktur über Namen"-Prinzip in CLAUDE.md. **WIEDERANLAUF.md hängt an der CLAUDE.md-Struktur — bei Regel-Umbauten mitprüfen** | `grep -rl "ROLLE:" *.md` listet 6 Dateien; ⚠️ **Umbenennen/Verschieben = Abhängigkeits-Änderung**: Marker mitnehmen, dieses Register + Komfort-Namen in WIEDERANLAUF.md/CLAUDE.md im selben Commit nachziehen |
| **Sanfter Wechsel** (`Mailbox.switch_pending`, 22.07.) | Modell-/Effort-Zweig in `_handle_keyboard_btn` (kein hartes `close_session` bei laufendem Job mehr), `_session_worker` (schließt nach Job-Ende), 5.2-Statuslogik (Job endet nie mehr als „fehler" durch Wechsel), Session-Aufbau via Prefs; künftig 5.21-Modell-Baustein (👍-Übernahme nutzt denselben Weg) | Wechsel im Leerlauf → sofort „aktiv. Session neu gestartet"; Wechsel bei laufendem Job → „🔄 Vorgemerkt…", laufende Antwort kommt vollständig an, nächste läuft im neuen Modus |
| **Fehler-Sofortmeldung** (`_notify_job_failed`, 22.07.) | Worker-Except-Pfad + Nicht-zustellbar-Pfad in `_run_job` — kein „fehler"-Job mehr ohne Nachricht an Adam | Job künstlich scheitern lassen → ⚠️-Meldung mit Job-Vorschau kommt sofort, Record bleibt für Reconcile liegen |
| **Repo-Bundle im Backup** (`scripts/vps_backup.sh`, 22.07.) | Notfall-Plan 4.1: datierte Offline-Vollkopien (14 Stück Rotation) unabhängig von den drei Live-Klonen | `git bundle verify ~/VPS-Backup/bundles/<neuestes>.bundle` → „complete history" |
| **Log-Sync-Kette** (23.07.): Deploy-Key `~/.ssh/id_ed25519_logsync` + SSH-Alias `github-logsync` + Klon `~/logsync/claude-bot-logs` + `scripts/log_sync.sh` + Timer `claude-log-sync.timer` | „Eigene Augen" der Kontroll-/Websitzung (liest Bot-Gespräche aus `claude-bot-logs` statt Screenshots); 4.2. ⚠️ Der Key darf NUR im Log-Repo schreiben — nie einen Bot-Repo-Key auf dem VPS ablegen (Angriffsfläche auf den Code) | `[VPS]` `systemctl list-timers claude-log-sync.timer` zeigt nächsten Lauf; `systemctl start claude-log-sync.service` → journalctl „Log-Sync gepusht"; GitHub: jüngster Commit „Log-Sync …" |
| **Gegenleser, ruhend** (29.08., Rang 8): `gegenleser.py` (ROLLE: `gegenleser`) + Prüfer `scripts/test_gegenleser.py` + Ablage `~/.claude/gegenleser/` (`GEGENLESER_DIR`, im Läufer geriegelt). **GEBAUT, NICHT SCHARFGESTELLT** | **Ein Gegenleser kann auf drei Arten versagen, und alle drei sehen von außen wie Erfolg aus.** (1) Der Dienst ist ausgefallen → fühlt sich an wie [keine Einwände]; deshalb ein eigener Zustand `AUSGEFALLEN`, der als OFFEN zählt, nie als grün — dieselbe Unterscheidung wie `bot.suchlage()` bei der Websuche. (2) Das Modell stimmt höflich zu → `zustimmung_ohne_substanz()` misst **zwei** Merkmale zusammen: kurz UND ohne Bezug auf seltene Wörter der Vorlage. Nur die Länge zu prüfen wäre falsch (ein knapper konkreter Einwand ist wertvoll), nur den Wortlaut auch ([Sieht gut aus, aber Zeile 40 …] ist eine echte Prüfung). (3) Der Befund erreicht niemanden → `ablegen()` meldet ein Misslingen, statt es zu verschlucken. ⚠️ **`zdr_lage`: beantragt ist NICHT bewilligt** — zwei Felder statt eines Hakens, denn ein nie bewilligter Löschantrag ist unsichtbar. ⚠️ **Das Modul ruft nichts auf** — kein Schlüssel, kein Netz, keine Kosten; der Aufruf wird hereingereicht wie die Geheimnis-Schranke bei `bashfreigabe`. 💰 **Vor dem ersten echten Aufruf: Ausgabenlimit BEIM ANBIETER** (10/10/5 EUR, Deckel 30 — Adams Entscheid 28.08.), dann ZDR, dann Rauchtest. `gpt-oss-120b` bleibt draußen (Adams Leitplanke, enge Auslegung ist die sichere) | `.venv/bin/python scripts/test_gegenleser.py` → 39 Zeilen, darunter sieben, die belegen, dass das Modul **keinen** Anbieter aufruft |
| **Bash-Dialog-Auswertung** (29.08.): `scripts/bash_dialog_auswertung.py` (ROLLE: `bash-dialog-auswertung`) + Ablage `~/.claude/bash-freigaben.jsonl` (`BASHFREI_PROTOKOLL`, im Läufer geriegelt) + `bashfreigabe.protokollieren` | **Adams Nachtrag vom 29.08., 01:2x macht die Messung zur Bringschuld** [Sie werden über kurz oder lang nerven. Messdaten proaktiv hinzuziehen bitte!] — der Befund kommt nach sieben Tagen von selbst, statt auf eine Nachfrage zu warten. ⚠️ **Modellfrei** (kein Claude-Aufruf, kein Netz) — sonst wäre der Zeitgeber AGB-Grauzone und kostenpflichtig. ⚠️ **Die Stoßrichtung der Vorschläge ist festgelegt und darf nicht kippen:** Wiederkehrer werden durch **benannte, geprüfte Skripte** ersetzt, die einzeln in die Liste rücken — **nie durch Öffnen einer Klasse**; `python3 <beliebig>` bleibt dialogpflichtig. Ein eigener Prüfer hält genau das fest. ⚠️ **Abgelegt werden nur Zeitpunkt, Urteil, Befehlsart, Bereich** — ausdrücklich KEIN Grund, denn der trägt Pfade. ⚠️ **Eine Ablage aus lauter unlesbaren Zeilen meldete erst [keine Aufrufe verzeichnet]** — eine kaputte Messung als ruhige Woche, die schlechteste Auskunft, die dieser Bericht geben kann. Vom Prüflauf gefunden, jetzt eigener Befundtext | `.venv/bin/python scripts/bash_dialog_auswertung.py` → Bericht; `beurteilen()` ist eine reine Funktion und wird in `scripts/test_bashfreigabe.py` ohne Datei und ohne Uhr gemessen |
| **Bash-Positivliste** (29.08.): `bashfreigabe.py` (ROLLE: `bash-positivliste`) + Prüfer `scripts/test_bashfreigabe.py`; **verdrahtet in `can_use_tool`** — `bashfreigabe.entscheiden` (bot.py:3064) und `.protokollieren` (bot.py:3077), beide innerhalb `can_use_tool`, die über `make_permission_callback` (bot.py:3808) hängt. Das Modul gibt selbst `Allow`/`Deny` zurück. **`[BERICHTIGT 29.08., Engywucks Ablage-Stichprobe]` Hier stand [noch nicht verdrahtet] — sechs Commits lang, ab der Verdrahtung in `8170b3a`. Die Fehlerrichtung ist die gefährliche: Dieses Register wird laut `CLAUDE.md` **vor jeder Änderung oder Entfernung einer Komponente** befragt. Wer die Zeile las, schloss: an `bashfreigabe.py` hängt nichts, da kann man schrauben — während es in Wahrheit zwischen 8.7-Schreibsperre und Freigabe-Dialog steht. **Ausgerechnet die Bezugs-Integrität, gegen deren Bruch das Register gebaut wurde, war im Register selbst falsch eingetragen.** Ursache: Ich habe die Zeile beim Bau des Moduls geschrieben und beim Verdrahten nicht nachgezogen — der Commit trug es im Titel, das Register nicht.** | **Die Wache steht am Ausgang statt am Auge.** Gemessen: 448 Bash-Aufrufe in sieben Tagen, 352 mit Dialog. Eine Rückfrage, die 352 Mal kommt und ungelesen bestätigt wird, schützt nicht — sie gewöhnt ab. ⚠️ **Die Geheimnis-Schranke wird HEREINGEREICHT, nicht nachgebaut** (`_is_sensitive_ref` bleibt in bot.py) — zwei Listen driften, eine gemeinsame kann es nicht (Lehre G1). ⚠️ **`Bereich` löst seinen Pfad SELBST auf:** Beim ersten Lauf waren elf Zeilen rot, weil `/var/folders` auf `/private/var` zeigt — im Betrieb wäre das eine Freigabe gewesen, die stillschweigend nie greift. ⚠️ **Nur eine Verkettungsform** (`cd <Bereich> && <Befehl>`); Ersetzungen, weitere Verkettungen, Umlenkung nach außen und unbekannte Verben gehen in den Dialog. ⚠️ **Programmausführung bleibt dialogpflichtig** (`python3`, `bash`, `node` …) — dieser Punkt trägt die ganze Konstruktion: Ein Skript kann jede Grenze umgehen. ⚠️ **Auflage A:** `.claude` ist zu, `.claude/memory` bleibt lesend frei — sonst widerspräche die Schranke dem System-Prompt, der genau das zusagt (8.7/Befund G) | `.venv/bin/python scripts/test_bashfreigabe.py` → **87 Zeilen** (die Zahl 64 stand hier und war der Stand vor Auftrag 5 und dem 8.7-Doppelboden — dieselbe Drift, andere Spalte); **Entkernungs-Gegenprobe über acht Schutzzeilen gefahren** (Schutz raus → rot, Schutz rein → grün). Sie hat zwei blinde Prüfzeilen aufgedeckt: Die Nutzlast trug eine URL, und deren Schrägstrich wurde von der Bereichsprüfung gefangen — die Zeilen maßen einen anderen Schutz als gedacht |
| **Anhang-Erkennung, Rang 2** (29.08.): `email_kanal.arten_aus_bodystructure` + `_lese_sexp` + `_teile_sammeln`; Prüfer `scripts/test_bodystructure_rang2.py` | **Die Struktur wird ZERLEGT, nicht durchsucht.** Vorher suchte ein regulärer Ausdruck je zwei Zeichenketten in Anführungszeichen — in einer BODYSTRUCTURE stehen dutzende solcher Paare, die keine MIME-Typen sind. ⚠️ **Der schwerste Fall: die Parameter im Content-Type schreibt der ABSENDER.** `charset=utf-8; image=x; audio=y; video=z` erzeugte vier Anhänge aus dem Nichts — damit stammte die Wortwahl in Adams Übersicht doch von ihm, und die Zusage aus Engywucks Punkt ④ (*nie der Dateiname, der MIME-Typ kommt vom Server*) war in der Umsetzung gekippt. ⚠️ **Nur die ersten beiden Stellen eines Teils** sind Typ und Untertyp; ein Parameter kann dort nicht stehen. ⚠️ **Bei einem Mehrteiler zählen nur die FÜHRENDEN Listen als Kindteile** — sonst wird `("boundary" "x")` als Teil gelesen und ergibt bei JEDER mehrteiligen Mail einen Anhang [unbekannt]. ⚠️ **fail-quiet:** Eine abgeschnittene Antwort sagt nichts, statt aus dem lesbaren Teil eine Behauptung zu machen | `.venv/bin/python scripts/test_bodystructure_rang2.py` → 16 Zeilen gegen Strukturen nach RFC 3501 §7.4.2; fünf Entkernungen gefahren, jede an der vorher notierten Zeile rot |
| **UID-Kennung, Rang 2** (29.08.): `email_kanal._abrufen` + `nachricht_text` + `als_text`; Aufrufer `bot.on_mail_knopf`; Prüfer `scripts/test_uid_kennung_rang2.py`. **Die Attrappe in `scripts/test_email_9_5.py` beantwortet nur noch `uid()`** — wer wieder über Positionen adressiert, fällt dort hin | **Eine Sequenznummer ist keine Kennung, sondern eine Position.** ⚠️ **Der Fehler sieht aus wie ein Ergebnis:** Verschwand zwischen Übersicht und Knopfdruck eine ältere Mail, holte Knopf n stillschweigend eine andere Nachricht als Zeile n zeigte. Mit der UID kommt stattdessen „die gibt es nicht mehr" — eine Auskunft. ⚠️ **Die UID allein heilt es nicht:** Liste und Knöpfe stammten aus ZWEI Abrufen, also zwei Zeitpunkten. Deshalb `als_text()` — ein Abruf, beide Hälften daraus. ⚠️ **Die Kennungs-Schranke musste mitwachsen:** `\d{1,9}` hätte UIDs über 999 999 999 abgewiesen; eine UID ist 32 Bit (max. 4294967295). ⚠️ **Eine leere UID-Antwort ist kein Fehler**, sondern die normale Auskunft für „gibt es nicht mehr" — sie wird benannt, nicht als leere Mail ausgegeben | `.venv/bin/python scripts/test_uid_kennung_rang2.py` → 21 Zeilen, ausgeführt gegen eine Attrappe ohne `search`/`fetch`; der Knopfweg wird über `bot.on_mail_knopf` wirklich gefahren und die Zahl der Anmeldungen gezählt. Fünf Entkernungen, jede an der vorher notierten Zeile rot |
| **Rangvermerk zuerst, Rang 2** (29.08.): `mailtext.bericht` (neue Schlüsselwörter `absender`/`betreff`) + `_kopfwert`; einziger Aufrufer `bot.mail_zusammenfassen`; Prüfer `scripts/test_rangvermerk_rang2.py` | **Der Fehler saß im AUFRUFER, nicht in der Funktion.** `bericht()` setzte den Vermerk seit jeher an den Anfang — `mail_zusammenfassen` hängte Absender und Betreff davor, also absenderkontrollierten Text ohne Einordnung. ⚠️ **Eine Zusage, die der Aufrufer umgeht, ist keine** — deshalb nimmt `bericht()` die Kopfzeilen jetzt selbst entgegen; der Aufrufer hat nichts mehr, was er davorhängen könnte. ⚠️ **Kopfzeilen bleiben EINE Zeile:** Ein Betreff mit Zeilenumbruch könnte `## Sichtbarer Text` an einen Zeilenanfang setzen und eine Abschnittsgrenze vortäuschen. ⚠️ Gekappt auf 200 mit sichtbarem `…` | `.venv/bin/python scripts/test_rangvermerk_rang2.py` → 16 Zeilen; der ganze Weg wird über `bot.mail_zusammenfassen` gefahren und die **tatsächlich abgeschickte** Eingabe gemessen, nicht nur der Baustein. Vier Entkernungen, jede an der vorher notierten Zeile rot |
| **Stundenblumen-Kette: Leser und Schreiber kennen verschiedene Orte** (Fächer-Funde [20] [35], 30.08.): Schreiber `scripts/stundenblume.py:66` respektiert `BLUMEN_DIR`, Leser `bot.py:4029` rechnet fest aus `Path.home()`. **Nicht repariert — bewusst auf der F-Liste** (Adams Entscheid: trifft nur Läufe mit gesetztem Umgebungsschlüssel, heute niemanden im Betrieb) | ⚠️ **Wer `BLUMEN_DIR` setzt, bekommt eine Falschauskunft:** Bei lückenloser, sekundenfrischer Kette meldet der Bot „noch keine Glieder (läuft der Zeitgeber?)" — ausgeführt gemessen. Die beruhigende Variante desselben Fehlers wäre schlimmer, diese ist immerhin beunruhigend. ⚠️ **Der Eintrag steht hier, weil die Reparatur eine Zeile wäre und der Fund sonst verschwindet** — nicht als Freibrief, ihn liegen zu lassen. Dasselbe Muster: `_USAGE_FILE` [39] (einziger Pfad ohne Umgebungsschlüssel, alle Nachbarn haben einen) und `botenpost.ziel_finden()` [36] | Beim nächsten Anfassen der Stundenblume: `os.environ.get("BLUMEN_DIR")` auch im Leser. Prüfbar durch einen Lauf mit gesetztem `BLUMEN_DIR` und frischer Kette — der Bot muss die Glieder sehen |
| **Governance-Hook fail-closed, A3** (30.08.): `.claude/hooks/guard-master-files.sh`; Prüfer `scripts/test_governance_hook.py` (24 Zeilen) | **Die vierte Schicht der Governance ließ sich durch einen Ausfall öffnen.** Der Dateipfad kam aus einem `python3`-Einzeiler mit `2>/dev/null`: kaputter Shim, dyld-Fehler, gedriftetes Eingabe-Schema oder unlesbares JSON endeten in **exit 0 und leerer Ausgabe — exakt wie beim legitimen Durchlassen.** In zwei Fassungen gab es keinen fail-closed-Zweig. ⚠️ Der Unterschied, auf den es ankommt: *gelungen + kein Pfad* (anderes Werkzeug, durchlassen) ↔ *misslungen* (wir wissen nicht, was geschrieben wird → Stopp). ⚠️ Ebenso: fehlendes `git` und ein misslungener `rev-list` (vorher `|| echo 0` — „die Kopie ist aktuell", die beruhigendste aller Falschauskünfte). ⚠️ **Offline bleibt bewusst offen**, sonst hängt die Arbeit an der Erreichbarkeit von GitHub. ⚠️ **Ein fremdes Projekt mit eigener CLAUDE.md wird durchgelassen** — ohne diesen Zweig wäre der Umbau dort ein Arbeitsverbot | `.venv/bin/python scripts/test_governance_hook.py` → von 16 auf 24 Zeilen; die Ausfälle werden **erzeugt** (PATH ohne python3, PATH ohne git, unlesbares JSON, Repo ohne HEAD) und der Rückgabewert gemessen. Fünf Entkernungen; **eine war zuerst falsch konstruiert** (halber Rückbau blockierte immer → drei fremde Zeilen rot statt der einen erwarteten), sauber wiederholt |
| **Geweitete Mengen im Zielumgebungs-Prüfer, A2** (30.08.): `scripts/test_zielumgebung.sh` (`$SKRIPTE` aus `git ls-files`, Rückfall `find`); `${HOME:?…}`-Schranke in `log_sync.sh`, `mac/icloud_backup.sh`, `mac/icloud_spiegel.sh`, `node_vollzug_pruefen.sh`, `vps_backup.sh`, `.claude/hooks/session-start.sh` | **Alle drei Schleifen sahen nur `scripts/*.sh`** — sieben versionierte Skripte lagen draußen, darunter **alle drei Hooks**. Bitter, weil dieser Prüfer genau gegen den 29.07.-Fehler gebaut wurde und ausgerechnet die Hooks nicht sah. ⚠️ **Der Filter warf zu viel weg:** `grep -v ':-'` verwarf auch `${VAR:-$HOME/x}` — dort wird `$HOME` expandiert, sobald VAR fehlt, und bricht unter `set -u` genauso. Fünf der sechs gefundenen Stellen hatten diese Form. ⚠️ **Repariert statt nur gemeldet:** `${HOME:?…}` bricht mit einem GRUND ab statt mit „unbound variable" mitten im Lauf. ⚠️ **Schleife 3 bleibt bewusst kurz** — sie *startet* Skripte, und ein Start ist eine Handlung (`guardian.sh` würde den Bot neu starten); aufgenommen wurde nur, was zeitgesteuert ohne Mensch läuft, dabei das bisher fehlende `log_sync.sh` | `bash scripts/test_zielumgebung.sh` → von 24 auf 38 Zeilen. Die Zeile **„die Menge ist vollständig"** vergleicht die geprüfte Anzahl gegen `git ls-files` — ohne sie wäre die Weitung ungeprüft, denn *eine fehlende Zeile fällt niemandem auf*. Vier Entkernungen: Menge zurück auf `scripts/*.sh` → 24/26 statt 37/38; Schranke entfernt → sechs Dateien rot; Filter-Rückbau → wie vorher notiert **wirkungslos**, deshalb separat mit eingebauter `${VAR:-$HOME}`-Zeile gemessen (erster Anlauf griff ins Leere, weil das Opfer kein `set -u` hatte) |
| **Übersprungen ist nicht bestanden, A1** (30.08.): `bot.NichtsGemessen` + `run_self_check`; `scripts/regressionstest.sh::run` (Rückgabewert **77**, Zähler `UEBERSPRUNGEN`, `mktemp` statt festem Pfad); `test_media_h1.py`, `test_log_sync_quittung.py`, `log_sync.sh` (rsync-Schranke), `test_zielumgebung.sh` (`melde skip`); Prüfer `scripts/test_uebersprungen_a1.py` | **Sechs Funde des Fächers sagten dasselbe: ein Prüfer meldet grün, ohne gemessen zu haben.** ⚠️ Der schwerste war der **Pin-Wächter selbst** — wer `==` auf `>=` lockert, erhöht das Rückfall-Risiko und schaltet im selben Zug die Wache ab. ⚠️ **Der Läufer war die halbe Ursache:** Er kannte zwei Ausgänge, also musste jeder Übersprung als bestanden durchgehen. ⚠️ **Nicht alles Stille ist ein Fehler:** `_c_stt_backend` (bewusst abgeschaltet) und `_c_log_repo_ampel` (noch nicht gebaut) bleiben stille `return` — im Code begründet. Wer sie mitumstellt, meldet eine gewollte Einstellung als Störung. ⚠️ **Sichtbarkeit statt Rot:** Ein Übersprung zählt nicht als bestanden, gilt aber nicht als Fehlschlag — daraus entsteht die Zahlendifferenz Mac (23/24) ↔ Zielumgebung (24/24), die bisher fehlte | `.venv/bin/python scripts/test_uebersprungen_a1.py` → 26 Zeilen über vier Ebenen, alle ausgeführt (`run()` und die Bilanzzeile werden aus dem Läufer geschnitten und gefahren; die Prüfer laufen in einem PATH ohne ihr Werkzeug). Acht Entkernungen — **zwei davon fanden blinde Flecken im Prüfer selbst**: die Bilanzzeile war nicht gemessen, und eine Verzweigung über das zu Messende machte die Zielumgebungs-Zeile blind |
| **Ablageweg fuer Gespraechsentscheidungen** (30.08., Claudias Auftrag 2): `freigaben.gespraechsentscheidung` + `scripts/entscheidung_ablegen.py` (ROLLE `entscheidung-ablegen`); uebertragen wird ueber den BESTEHENDEN `scripts/entscheidungs_protokoll.py`, nicht ueber einen zweiten Weg | **Das Protokoll war gebaut und LEER** - gemessen am 28.08.: null Eintraege, Ordner seit dem 25.07. unveraendert. Kein Fehler, ein Zuschnitt: Es erfasste nur Urteile aus dem Freigabe-Postfach, Adams Entscheidungen fallen aber ueberwiegend frei im Gespraech. Der eigene Kopfsatz galt gegen es selbst. ⚠️ **Handgriff, nicht Automatik** - Claudia hat die Frage offen gelassen, gebaut ist die sichere Haelfte. Automatik erzeugte erfundene Urteile; der Fall ist belegt: Ihre erste Formulierung machte aus Adams BEDINGTER Ablehnung eine grundsaetzliche. ⚠️ **Das Zitat ist Pflichtfeld** - ohne Zitat keine Zeile, denn eine zusammengefasste Entscheidung ist eine Auslegung. ⚠️ **Die Tabelle bleibt sechsspaltig** - das Zitat wandert in die Grund-Spalte; eine siebte Spalte braeche jede bestehende Drehbuchzeile | `.venv/bin/python scripts/test_freigaben_9_4.py` -> drei Zeilen: Ablage, Pflichtfeld, Zitat in der Drehbuchzeile. Drei Entkernungen, jede an der vorher notierten Zeile rot |
| **Zustandspruefer der Ausarbeitungen** (30.08., Claudias Auftrag 1 gegen Karteileichen): `scripts/ausarbeitungen_pruefen.py` (ROLLE `ausarbeitungen-pruefer`), verdrahtet in `scripts/daily_check.sh` (9g), Zustand `~/.claude/ausarbeitungen-stand.json`; Riegel in `regressionstest.sh`; Pruefer `scripts/test_ausarbeitungen.py` | **Eine Entscheidung wurde angenommen, aber nicht abgelegt** - das Gespraech verging, das Dokument blieb, und beim naechsten Zugriff gewann das Dokument. Register verzeichnen, was EXISTIERT; ein ueberholter Satz in einem gueltig aussehenden Papier existiert weiterhin. ⚠️ **Was er NICHT kann, und das steht im Modulkopf:** Claudias eigenen Vorfall haette er nicht gefunden - das Papier war einen Tag alt. Er misst Alter und Anwesenheit eines Kopfes, nicht Inhalt; ihr Auftrag 2 ist deshalb der wichtigere. ⚠️ **Die Daempfung ist der Gegenstand:** erster Lauf nur eine Zahl, danach nur Zugaenge - sonst stuenden dort taeglich 45 Namen und die Meldung waere binnen zwei Tagen ueberlesen. ⚠️ **Der Kopf steht OBEN (zehn Zeilen):** Im Bauauftrag selbst steht ein Beispielblock mit drei STAND-Zeilen in Zeile 61 - ein Pruefer ueber die ganze Datei haette ihn als Kopf gezaehlt und das Papier faelschlich als gepflegt gefuehrt. ⚠️ Empfaenger ist die **Kontrolle** ueber das Auftragsbuch, nicht Adam - eine taegliche Zeile ueber Papierpflege kostet Aufmerksamkeit fuer die Zeilen, die zaehlen | `.venv/bin/python scripts/test_ausarbeitungen.py` -> 16 Zeilen gegen einen Wegwerf-Ordner, jede Fundart mit ihrer Gegenrichtung; der Auftragsbuch-Weg wird als Unterprozess ausgefuehrt |
| **Der dritte Knopf „Ändern"** (30.08., Claudias Bauauftrag vom 28.08.): `freigaben.Anfrage.art/geaendert_*/aenderung_*`, `aendern`, `aenderung_beginnen`, `aenderung_zu_nachricht`, `ARTEN`, `AENDERUNG_FRIST_S`; `bot._freigabe_anzeigen` (drei Knöpfe + Kopier-Knopf + Art-Zeichen), `on_freigabe_callback` (Zweig `frg:aendern`), `_freigabe_aendern_uebernehmen`, Abfang in `on_message`; Prüfer `scripts/test_freigaben_9_4.py` | **Der Knopf verändert den Text, über den anschließend entschieden wird — das ist der Angriffsweg, nicht der Komfort.** Fünf Auflagen: ⚠️ **(1)** nur Adams Kennung, gegen die Allowlist, nicht gegen den Chat. ⚠️ **(2)** nach jeder Änderung wird ERNEUT vorgelegt — sonst stünde zwischen Änderung und Freigabe womöglich etwas anderes da, als Adam gelesen hat. ⚠️ **(3)** sichtbar, auch im Protokoll (`formuliert_von`) — *eine von Adam selbst formulierte Zeile ist stärker als meine*. ⚠️ **(4)** die Geheimnisprüfung läuft auf dem neuen Text ERNEUT; sie griff bisher nur beim Anlegen — die Leitplanke war offen, wo sie niemand vermutet. ⚠️ **(5)** nur an offenen Anfragen. ⚠️ **Variante B, nicht Adams Idealbild:** Ein Fenster mit Textfeld bräuchte eine öffentlich erreichbare Adresse mit TLS — neue Angriffsfläche für kleinen Zugewinn. ⚠️ **`reply_markup` trägt Knöpfe ODER erzwungenes Antworten** — deshalb sitzt der Kopier-Knopf an der Freigabe-Box, das ForceReply an der Änderungs-Nachricht; Claudias zwei Kopierwege bleiben. ⚠️ **Kopier-Knopf nur bei ≤250 Zeichen** — ein still gekappter Kopiertext wäre derselbe Fehler wie stilles Abschneiden. ⚠️ Eine hängende Änderung löst sich nach `AENDERUNG_FRIST_S` auf und legt den URSPRÜNGLICHEN Text erneut vor — sonst sperrte der Merker die Anfrage für immer | `.venv/bin/python scripts/test_freigaben_9_4.py` → 11 neue Zeilen, jede Auflage einzeln; **der Bot-Weg wird ausgeführt** (fremde Kennung, Knopfdruck, Antwort mit Bezug) und dabei gemessen, dass Adams Fassung **nicht** an den Agenten geht. Acht Entkernungen, jede an der vorher notierten Zeile rot |
| **Der dritte Rückgabewert (A1, Signalstufe)** (30.08.): `scripts/regressionstest.sh` gibt **77** zurück, wenn `FAILS=0` und `UEBERSPRUNGEN>0`; verarbeitet in `daily_check.sh`, `hora.py::regression`, `updater.py::_regression`, `node_vollzug_pruefen.sh` | **Die Zahl war ehrlich, das Signal nicht.** Ein Lauf mit 65 Übersprüngen endete mit `exit 0`, und alle vier Verbraucher gingen auf grün. ⚠️ **Der schwerste ist `hora.py`:** Ein `True` öffnet BEIDE Tore — „auf rotem Fundament wird nicht gearbeitet" und das Abhaken danach; ein Läufer, der autonom Befehle ausführt, hätte auf unvermessenem Fundament gearbeitet und quittiert. ⚠️ **Drei Zustände: 0 = alles gemessen und grün · 77 = grün, aber nicht alles gemessen · sonst = Fehlschläge.** Wer nur „ungleich null" prüft, fährt automatisch die sichere Richtung — deshalb trug `updater.py` schon vorher, ihm fehlte nur der Grund. ⚠️ **Ein Übersprung lässt den Lauf NICHT scheitern** (eine Maschine ohne ffmpeg ist nicht rot) — er ist nur nicht grün | In `scripts/test_uebersprungen_a1.py` (40 Zeilen): der Schlussblock des Läufers wird geschnitten und mit vier Zählerlagen gefahren; `hora.regression()` und `updater._regression()` laufen **ausgeführt** gegen eine Läufer-Attrappe, die 77 liefert. Die zwei Shell-Verbraucher bleiben Textsuche — benannte Grenze, `daily_check.sh` ruft über `sudo -u`. Vier Entkernungen; die bei `hora.py` zeigte, dass der Riegel im **Läufer** sitzt und Horas Zweig nur den Grund liefert |
| **Stiller Erfolg zählt als Übersprung (A1, zweite Stufe)** (30.08.): Häkchen-Schranke in `scripts/regressionstest.sh::run`, begrenzt auf Argumente unter `scripts/*.py`/`*.sh` | **Der 77er-Weg verlangt, dass ein Prüfer seinen Übersprung selbst bemerkt — genau das taten die sechs Fächer-Fälle nicht.** Diese Schranke fängt die Klasse unabhängig davon: wer kein Häkchen gedruckt hat, hat nichts gemessen. ⚠️ **Sie gilt NUR für eigene Prüfskripte, und das ist eine Messung, keine Vorsicht:** Alle 54 Skript-Prüfer drucken Häkchen, die zehn `py_compile`-Läufe nicht — dort ist Schweigen die Konvention. Der erste Anlauf ohne diese Unterscheidung meldete **zehn falsche Übersprünge je Lauf**; ich hatte die Menge gemessen, die mir am Bautag einfiel, nicht die tatsächliche | In `scripts/test_uebersprungen_a1.py` (29 Zeilen): fünf Prüflinge durch die geschnittene `run()` — grün, gemeldeter Übersprung, rot, **stiller Erfolg**, **fremdes Werkzeug**. Beide Richtungen gemessen; zwei Entkernungen, jede an der vorher notierten Zeile rot |
| **Keine Verknüpfung aus Fremdtext, Rang 2** (29.08.): `email_kanal._VERKNUEPFUNG_RE` + `_neutral`; Prüfer `scripts/test_verknuepfung_rang2.py` | **Klartext schützt nicht — der Telegram-Server verlinkt selbst.** `url`, `email` und `phone_number` sind eigene Entitätstypen, die ohne `parse_mode` automatisch gesetzt werden; die Zusage „keine anklickbare Adresse" hing an der Annahme, ohne Auszeichnung bleibe alles Text. ⚠️ **Der Schaden ist die Verknüpfung, nicht die Adresse:** Ein Link im Betreff steht in einer Nachricht, die vom Bot kommt — er sieht vertrauenswürdiger aus als in der Mail. ⚠️ **Bewusste Restlücke:** schemalose Adressen (`boese.tld`) bleiben erkennbar; jede Regel dafür („Wort Punkt Wort") frisst echten Wortlaut — „Herren.Ihre Bestellung" wäre nach ihr eine Verknüpfung. Im Code benannt, nicht verschwiegen | `.venv/bin/python scripts/test_verknuepfung_rang2.py` → 27 Zeilen, **beide Richtungen**: nichts Verlinkbares kommt durch UND harmloser Wortlaut bleibt unangetastet (ohne die zweite Hälfte wäre die erste mit `return ""` zu erfüllen). Drei Entkernungen, jede an der vorher notierten Zeile rot — die dritte (gieriger Ausdruck) fällt prompt über „Nr. 42" |
| **Node-Vollzugsprüfer** (29.08.): `scripts/node_vollzug_pruefen.sh` (ROLLE: `node-vollzug-pruefer`) + Stand `~/.claude/node-vollzug-vorher.txt`; gehört zu `docs/auftraege/ZETTEL-node-22-auf-24.md` | **Kein Prüfer im Regressionslauf, sondern ein Werkzeug für den Vollzug** — zwei Aufrufe (`vorher` / `nachher`) statt sechs Handgriffen mit Augenmaß, während der Dienst steht. ⚠️ **Er springt NICHT** — kein apt, keine Paketquelle, kein npm; der Vollzug bleibt Adams Hand (root). ⚠️ **Ohne Vorher-Stand verweigert er die Arbeit**: Ein Vergleich gegen nichts sieht aus wie ein bestandener. ⚠️ **Er misst die beiden CLIs GETRENNT** — `claude_system` (npm-global, 2.1.209) und `claude_bot` (die gebündelte 2.1.219, die das SDK tatsächlich startet). Nur die zweite trägt den Betrieb; wer sie zusammenwirft, baut einen Fehlalarm. ⚠️ **Beide `package.json` per md5**, weil ein globales npm-Update je nach Präfix nur einen der beiden Orte erwischt — dann zeigt der Verweis auf die alte Fassung, während die Anzeige etwas Plausibles sagt | `[VPS]` `bash scripts/node_vollzug_pruefen.sh vorher` → zeichnet auf; `… nachher` → vergleicht jede Größe einzeln und fährt den Regressionslauf. Ohne echten Sprung meldet er korrekt [der Sprung hat nicht gewirkt] |
| **Governance-Hook, dritte Schicht** (Prüfer neu 29.08.): `.claude/hooks/guard-master-files.sh` + Prüfer `scripts/test_governance_hook.py` | **Diese Schranke hatte keinen Prüfer — eine Schranke ohne Prüfer ist eine Bitte.** Sie blockiert Schreibzugriffe auf `MIGRATION.md`/`CLAUDE.md`, solange die Arbeitskopie hinter dem Master steht. ⚠️ **`case` in bash vergleicht schreibweisenempfindlich, und wer nicht passt, fällt in `*) exit 0` — also DURCHLASSEN.** Auf Adams Mac ist das Dateisystem schreibweisen-**un**empfindlich: `claude.md` trifft dieselbe Datei, umging aber beide Muster. **Die dritte Schicht stand ausgerechnet auf der Maschine offen, auf der die führende Sitzung schreibt**; auf VPS und Container entstand kein Loch, weil die Datei dort unter dem anderen Namen nicht existiert (Engywucks Fund ③). ⚠️ **`tr` statt `${VAR,,}`** — `/bin/bash` ist auf macOS die Fassung 3.2, die Kleinschreibungs-Erweiterung kam mit 4.0; eine Lösung, die nur auf einer Maschine läuft, wäre hier besonders absurd | `.venv/bin/python scripts/test_governance_hook.py` → 16 Zeilen an einem echten Wegwerf-Repo, das nachweislich hinter seinem Ursprung steht; sieben Schreibweisen müssen blockieren, fünf Nachbarn durchlaufen, und bei aktuellem Stand blockiert nichts |
| **iCloud-Zweitkopie** (29.08., Ersatz für den toten Mai-Spiegel): `scripts/mac/icloud_backup.sh` (ROLLE: `icloud-backup`) + Aufruf im `session-start.sh`-Hook + Tagesmarke `~/.claude/icloud-backup.tag` + Protokoll `~/.claude/icloud-backup.log` | **Nur auf dem Mac.** Sichert den ganzen iCloud-`KI`-Ordner nach `~/Backups/iCloud-Mirror/KI`. ⚠️ **Kein Zeitgeber, und das ist der Kern:** Der Vorgänger `com.jakuna.mirror-ki` lief alle fünf Minuten unter `launchd`, erbte Adams iCloud-Freigabe nicht und stand **seit dem 25.05. auf Rückgabewert 78 — 330 Läufe, drei Monate unbemerkt.** Am Sitzungsstart läuft das Skript unter Claude Code, also unter der App mit der Freigabe, und braucht **keine neue Berechtigung** (insbesondere keinen iCloud-Zugriff für `/bin/bash` — das ist die Shell, nicht eine App). ⚠️ **Höchstens ein Lauf je Kalendertag** (`--jetzt` erzwingt einen): Ein rsync über den ganzen Ordner bei jedem Start würde bremsen, und was bremst, wird abgeschaltet. ⚠️ **Kein `--delete`** — was in iCloud verschwindet, bleibt hier liegen; das versehentliche Löschen ist genau der Fall, gegen den die Kopie existiert (iCloud spiegelt Löschungen auf alle Geräte). ⚠️ **Keine Geheimnis-Bremse und kein Dublettenschutz**, anders als beim Papierweg: Ein Backup sichert alles, und Namen sind dort die Ordnung. Erster Lauf: **221 von 368 Dateien waren ungesichert** | `[Mac]` `bash scripts/mac/icloud_backup.sh --jetzt` → Exit 0 und frische Zeile im Protokoll; ein Fehlschlag gibt 78 und schreibt FEHLER — der Unterschied zum Vorgänger, der schwieg |
| **iCloud-Auftragsspiegel** (29.08., Adams Weg 3): `scripts/mac/icloud_spiegel.sh` (ROLLE: `icloud-spiegel`) + Aufruf im `session-start.sh`-Hook **und auf Zuruf** — der LaunchAgent `scripts/mac/com.jakuna.icloud-spiegel-bot.plist` liegt noch im Repo, ist aber **ausgetragen und wird nicht mehr geladen** (siehe unten) + Protokoll `~/.claude/icloud-spiegel.log` | **Läuft nur auf dem Mac, nie auf dem VPS.** Holt Engywucks und Claudias Papiere aus iCloud in `docs/auftraege/`, nachdem das Kopieren über den Chat zweimal an einem Tag Arbeit aufgehalten hat. ⚠️ **TCC gilt für den verantwortlichen Prozess, nicht für den Ordner — und das ist GEMESSEN, nicht vermutet:** Der LaunchAgent lief, meldete aber alle fünf Minuten `nicht lesbar (TCC?)` und Rückgabewert 78. Er erbt die Freigabe von `Claude.app` nicht. **Adams Entscheid vom 29.08.: Zeitgeber entladen, Aufruf an den Sitzungsstart** — dort läuft er unter der App mit der Freigabe, ohne dass irgendwo eine breitere Berechtigung gesetzt wird. ⚠️ **Dublettenschutz über den INHALT**, nicht über den Namen: Papiere kommen aus iCloud oft zusammengeschrieben an, während im Projekt die Bindestrich-Fassung liegt und referenziert wird — ohne die Prüfung wüchse der Ordner bei jedem Umbenennen um eine inhaltsgleiche Kopie, und **schlimmer als eine tote Datei ist eine zweite lebende**. Scheitert er, steht das als Klartextzeile im Protokoll und der Rückgabewert ist 78. ⚠️ **Jeder Lauf schreibt eine Zeile, auch der leere** — damit ist eine nicht mehr wachsende Logdatei selbst das Alarmzeichen. Der Vorgänger `com.jakuna.mirror-ki` (Mai, nach `~/Backups`) tat das nicht und steht **seit dem 25.05. auf Exit 78, bei laufendem Fünf-Minuten-Takt** — drei Monate stiller Ausfall. ⚠️ **Kopiert, löscht nie** (kein `--delete`), überschreibt nur Neueres. ⚠️ **Geheimnis-Bremse auf LÄNGE, nicht auf Präfix:** Die erste Fassung suchte `sk-ant-` und bremste 2 von 62 Dateien — beide Fehlalarme, beide *Prüfberichte über* Zugangsdaten. In diesem Projekt wird ständig über Geheimnisse geschrieben; das Präfix trennt nicht, die Länge des Restes schon | `[Mac]` `bash scripts/mac/icloud_spiegel.sh` → Exit 0 und frische Zeile im Protokoll; Gegenprobe beider Richtungen: echter Schlüssel wird gebremst, Bericht über einen Schlüssel geht durch |
| **Doku-Spiegel-Prüfskript** `scripts/check_hilfe_buttons.py` (8.6b, 23.07.) | Doku-Spiegel-Regel (CLAUDE.md): /hilfe ↔ Handler ↔ Menü ↔ Tastatur dürfen nicht driften. ⚠️ Das Skript parst `BotCommand(`/`CommandHandler(`-Muster und die `cmd_hilfe`-Quelle — wer diese Strukturen umbenennt, muss das Skript mitziehen | `.venv/bin/python scripts/check_hilfe_buttons.py` → Exit 0 „Doku-Spiegel konsistent"; ab 8.1 im 4-Uhr-Check |
| **8.7-Schreibschutz-Kette**: Edit/Write-Deny + `_is_repo_write_cmd()` (Bash-Seitenweg) + Selbstcheck „Repo NUR-LESEN" (VPS: `git status --porcelain` leer). **[GEÄNDERT 24.07.]:** dazu `_is_repo_read_cmd()` (Lesen frei) + `_REPO_DIR` als Read-Basis | Governance „lesen ja, schreiben nie". ⚠️ **Drei Wege, alle prüfen:** Lesen OFFEN (Read/Grep/Glob im Repo + verkettungsfreie Bash-Lese-Befehle), Schreiben ZU (Deny sitzt VOR Always-Allow — Reihenfolge nicht ändern), Geheimnis ZU (`_is_sensitive_ref` auch fürs Lesen). `_is_repo_read_cmd` verlangt: kein Shell-Metazeichen, kein Schreibmuster, kein Geheimnis-Pfad — wer das lockert, öffnet einen Schreib-/Fremdbefehl-Weg | Selbstcheck-Zeile grün (prüft Lesen offen + Schreiben zu + Geheimnis zu); live: `ls`/`cat` Repo ohne Dialog, `git commit` abgelehnt |
| **5.25-Recherche-Kette** (23.07.): `task_origins` (befüllt in `_run_job` + `stream_response`/ToolResultBlock) + `_extract_hosts`/`_url_host` + `_is_sensitive_ref` + `_tool_trace_line` + Prefs `always_allow`/`raw_tools` + `/freigaben`/`/technik` | Recherche ohne Freigabe-Klicks; Sicherheit hängt an der Reihenfolge im Permission-Callback: **Geheimnis-Check VOR Always-Allow VOR Herkunfts-Schranke VOR Workspace-Lesen** — wer dort umsortiert, öffnet Secrets oder fremdgesteuerte Abrufe. `task_origins` MUSS pro Aufgabe frisch aufgesetzt werden (wachsende Liste höhlt die Schranke aus) | Selbstcheck-Zeile „Reibungslose Recherche (5.25)" grün (prüft auch die Reihenfolge); live: Recherche ohne Klick, Verweis-Folgeabruf fragt nach, `.env`-Lesen fragt nach |
| **/spur-Kette** (5.25 d, 24.07.): `cmd_spur` + Pref `trace_off` (per user_id, **Default aus**) + `_trace_off()` + Wrap um die 🔧-FYI-Zeile in `stream_response` (Z. ~6225) + `UserSession.user_id` | Werkzeug-Spur ganz aus/an. ⚠️ Der `trace_off`-Check umschließt NUR die FYI-ToolUseBlock-Zeile — die Allow/Deny-Permission-Prompts (`_tool_trace_line` im Permission-Zweig) bleiben IMMER sichtbar; wer den Check verschiebt, darf die Rückfragen nicht mit stummschalten. Keyen auf `sess.user_id`, nie `chat_id` | `/spur` togglet; Selbstcheck/Doku-Spiegel grün (`check_hilfe_buttons.py` kennt /spur); live: /spur → 🔧-Zeilen weg, Allow/Deny kommt weiter |
| **Voice-Retry + Fehlerlog** (5.15, 24.07.): `_with_one_retry` um `voice.get_file`/`_download_tg_file` + `_log_bot_error`/`_BOT_ERROR_LOG` (`logs/bot-errors.log`) + `log_sync.sh` kopiert es mit | Voice-Robustheit; „eigene Augen" der Kontrollsitzung ohne journalctl. ⚠️ Retry NUR im Voice-Download-Pfad (kein globaler Retry); Fehlerlog-Pfad hängt an `LOG_DIR.parent` — wer LOG_DIR/Conversations-Pfad verschiebt, muss `_BOT_ERROR_LOG` + `LOG_SYNC_ERRLOG` mitziehen | `logs/bot-errors.log` wächst bei Voice-Timeout; nach Log-Sync im Repo als `bot-errors.log` sichtbar |
| **Pin-Divergenz-Wächter** (C2, 25.07.): Selbstcheck-Zeile „Pin-Divergenz (C2)" — vergleicht `requirements.txt`-Pins mit den TATSÄCHLICH installierten Versionen (`importlib.metadata`) | Netz gegen stille Rebuild-Rückfälle (Pin sagt 0.2.118, installiert 0.2.127 → Rebuild fiele zurück). ⚠️ Deterministisch, ohne Netz; meldet nur, was in DIESER Umgebung installiert ist. Erster Lauf fand sofort die Mac↔VPS-Divergenz (0.2.87 vs. 0.2.127) — Mac angeglichen. Ersetzt NICHT den Nachzieher (C1), sondern sichert ihn ab | Selbstcheck-Zeile grün; künstlich: Pin ändern → Zeile schlägt an |
| **Updater** (5.21-Gegenstück, 24.07.): `scripts/updater.py` (importiert `version_monitor`) + `/updates`-Befehl + `on_update_callback` (`upd:`) | Update-Anwendung nach Ampel. ⚠️ **Deterministisch, kein Modell-Aufruf; Vier-Augen (Freigabe-Button Pflicht); gepinnte Pakete (🟡) nie automatisch; Health-Check IST `regressionstest.sh` (kein zweiter); Rollback auf eingefrorene Version Pflicht — ohne Rollback kein Update.** Bot installiert nur pip-Pakete (npm/node/OS = manuell/root). Nach Erfolg Neustart nötig (neue Versionen laden) | `.venv/bin/python scripts/updater.py` listet Ampel; `/updates` im Bot zeigt Knöpfe; Selbstcheck/Doku-Spiegel kennen /updates |
| **PDF-Kette** (C, 24.07.): `pandoc` + `weasyprint` (apt) + `scripts/md2pdf.sh` + `scripts/pdf-style.css` (DejaVu Sans) | Datei-Regel „immer Original + PDF" auf dem VPS erfüllbar. ⚠️ Helvetica existiert auf Linux NICHT — Font bleibt DejaVu (in pdf-style.css); pandoc-Template-CSS-Warnungen (gap/overflow-x) sind kosmetisch. Kette bewusst schlank (kein LaTeX/Chromium) | `[VPS]` `bash scripts/md2pdf.sh test.md` erzeugt PDF; Abnahme 24.07.: Original+PDF via Postfach bei Adam angekommen |
| **Boten-Postfach** (B, 24.07.): `postfach_worker` (Hintergrund-Task) + `_postfach_send_one` + `_postfach_target_ok` + `_POSTFACH_DIR` (`~/postfach/outbox|sent|failed`) | Instanzen versenden über den Bot ohne Token. ⚠️ **Sicherheit:** Ziel-Allowlist (Adam/Ausgabekanal/Haus) + Geheimnis-Dateien (`_is_sensitive_ref`) werden NIE versendet — wer eins der beiden lockert, öffnet Exfiltration/Fremd-Versand. Ableger müssen atomar ablegen (.tmp→.json). Doku: `docs/boten-postfach.md` | Selbstcheck „Boten-Postfach (B)"; live: JSON in `outbox/` → kommt an, wandert nach `sent/`; Geheimnis-Datei → `failed/` |
| **5.21 Versions-Monitor** (24.07.): `components.json` (E5-Register) + `scripts/version_monitor.py` + `claude-version-monitor.timer` (Mo 04:20) + `logs/version-monitor.log` (Log-Sync) | Aktualitäts-Überwachung. **Deterministisch, KEIN Modell-Aufruf** (AGB); Quellen PyPI/npm/nodejs.org (kostenfrei). ⚠️ **Jede neue versionierte Komponente MUSS in `components.json`** (sonst ungeprüft — E5-Pflichtfeld jeder „fertig"-Definition). Installation bleibt manuell (E3); Major-Sprünge markiert. Meldet nur bei Updates | `[VPS]` `python3 scripts/version_monitor.py` listet cur→latest je Eintrag; `systemctl list-timers claude-version-monitor.timer` |
| **LobeChat 3.1** (24.07.): Docker (`docker.io`, enabled) + Container `lobehub/lobe-chat` (`--network host`, `--restart always`, `--env-file /etc/lobe-chat.env`) | Web-Interface (Phase 3), Backend LiteLLM→Ollama. 🔴 **Sicherheits-Invariante:** bindet NUR `127.0.0.1:3210` (via `HOSTNAME=127.0.0.1`) — **wer die HOSTNAME-Env entfernt oder `--network host` gegen `-p 0.0.0.0:...` tauscht, öffnet ein OpenClaw-Panel.** Kein ufw-Port für 3210. Zugang nur SSH-Tunnel (`docs/lobechat-zugang.md`). Kein Claude-Abo-Zugang (nur lokale Modelle). Drittanbieter-Image | `[VPS]` `ss -tlnp\|grep 3210` MUSS `127.0.0.1` zeigen (nie `0.0.0.0`); `ufw status\|grep 3210` = leer; `docker ps\|grep lobe-chat` = Up; `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:3210` = 307 |
| **8.1 Täglicher Check** (24.07.): `scripts/daily_check.sh` + `claude-daily-check.timer` (04:10) + `logs/daily-check.log` (Log-Sync kopiert es mit) | Selbstüberwachung. **Streng deterministisch — KEIN Modell-Aufruf** (AGB). ⚠️ Prüft Dienste/Instanz/Regressionstest/Token-Alter/Webhook-`pending`; Webhook-Gesundheit hängt an `pending_update_count`, NIE am Fehler-Alter (Startup-Race = Fehlalarm). Meldet nur bei Problemen. Nutzt `sudo -u claudebot` für den Regressionstest (venv) → root-Timer nötig | `[VPS-root]` `bash scripts/daily_check.sh` → „Alles grün" ohne Telegram-Meldung; `systemctl list-timers claude-daily-check.timer` |
| **Webhook LIVE** (1.9, 24.07.): `main()`-Webhook-Block Self-Signed direkt auf IP `159.195.195.82:8443`; Cert/Key `/etc/claude-telegram-bot-webhook/` (claudebot, Key 640); Envs BOT_MODE/WEBHOOK_URL/_SECRET_TOKEN/_PATH/_CERT/_KEY/_LISTEN_PORT in `/etc/claude-telegram-bot.env`; ufw 8443 nur Telegram-Netze | Eingehende Updates (statt Polling). ⚠️ **Kette:** stirbt eine Komponente (Cert gelöscht, ufw-Regel weg, Port belegt), kommen keine Nachrichten mehr an — Symptom: `getWebhookInfo` `last_error` wächst. Rollback = BOT_MODE+Webhook-Envs entfernen → Neustart → Polling. Cert läuft 2036 ab (dann neu erzeugen). Nie zwei Bots (Webhook + Polling) auf denselben Token | `[VPS-root]` `getWebhookInfo`: url gesetzt, has_custom_certificate true, last_error eingefroren; `ss -tlnp\|grep 8443` lauscht; lokal falsi­fiziert: falsches Secret → 403 |
| **Phase-6-Kanal-Kette** (24.07.): `channels.py` (HOUSES/ROUTES + detect_house/resolve_route/register_house/record_topic) + `_provision_house` (6.5, create_forum_topic ratenbegrenzt) + `on_my_chat_member`-Gruppenzweig + `route_target()` + `QueuedJob.output_thread_id` + `_run_job`-Thread-Auflösung + Prefs-Key `channels.houses` | Auto-Zimmer-Anlage bei erkannter Forum-Gruppe; Auto-Routing (Status→Werkstatt/Migration&Technik, Recherche→Bibliothek/Recherchen&Referenzen, Unzugeordnet→Werkstatt/Offene Punkte). ⚠️ `resolve_route` fällt NIE auf den Alt-Ausgabekanal zurück (None = im Bot-Chat bleiben); `detect_house` nimmt Jakuna-San bewusst AUS; Zimmernamen in `channels.HOUSES` = 4.3-Ordnernamen (`folder_name`) — beide zusammen ändern. Struktur-Hoheit: `docs/entscheidungsvorlagen/6-6-kanal-struktur-vorlage.md` (v3) | `.venv/bin/python scripts/test_channels_6.py` (7 Teilprüfungen); Selbstcheck-Zeile „Phase-6-Kanalstruktur (6.5/6.1)"; live (nach Adams Gruppen): Gruppe „🔧 Werkstatt" mit Forum-Modus anlegen + Bot einladen → 4 Zimmer erscheinen |
| **5.9-Reaktions-Kette** (23.07.): `reactions.py` (Vokabular v2.1 + VS16-Normalisierung + Fragen-Registratur `logs/open_questions.json`) + `on_reaction` + Registrierung in `send_answer_to_user` + `on_option_callback` (`opt:`) + `/stopp` | Emoji-Antworten auf ALLE Bot-Fragen; Permission-Flow hat **Vorrang** (👍/👎 auf wartende Freigabe erzeugt NIE einen 5.9-Job); 5.2 persistiert Reaktions-Jobs. ⚠️ Bezugs-Text hängt an `BOT_MSGS`/Registratur — beide halten bei >4096-Zeichen-Antworten nur EINE message_id (Registratur: die letzte; BOT_MSGS: die erste) — bekannte v1-Grenze, Reparatur mit der 8.5-v2-Datenlage. Vokabular-Änderungen IMMER zuerst in `reaktionen-vokabular.md`, dann in `reactions.VOCAB` — nie auseinanderlaufen lassen | Selbstcheck-Zeile „Emoji-Reaktionen (5.9)" grün; `scripts/test_reactions_5_9.py` (6 Teilprüfungen); live: 👍 auf Bot-Frage → Antwort kommt als Reply |
| **🎯 Gründlich-Modus** (Opus + Max-Effort) | **Pre-Send-Hook v2** — der Sicherheits-Gegencheck läuft dort (starkes Modell), bewusst NICHT in Phi-4-Mini | 🎯-Button in der Tastatur vorhanden; v1-Hook unabhängig davon lauffähig |
| **`_COST_TOOLS` (💰-Einzeldialog-Zwang)** | 💰-Kostenregel im Bot: WebSearch ist seit 23.07. **Notfall-Option statt hart deaktiviert** (Adam-Entscheid „Variante 2") — JEDE Nutzung erzwingt den 💰-Dialog mit Kostenhinweis, nie Always-Allow. ⚠️ Wer `WebSearch` aus `_COST_TOOLS` entfernt, macht die bezahlte Suche klicklos | Selbstcheck 5.25 prüft `WebSearch in _COST_TOOLS`; live: WebSearch anfordern → 💰-Dialog OHNE Always-Knopf |
| **Memory-Auto-Allow + `add_dirs`** | Session-Diät (5.23): Agent liest Detailwissen selbst nach. Fehlt → Rückfragen bei jedem Memory-Read oder gar kein Zugriff | `add_dirs` enthält Memory-Pfad; Read im Memory-Ordner ohne Permission-Prompt |
| **`stream_response()` SAMMELT nur** (seit 17.07., Vorstufe 5.8) | **Jede** Aufrufstelle muss den Rückgabetext selbst senden: `_run_job` (Hauptpfad), `_presend_gate` (Korrekturrunde), `_do_autorun` (Startup-Tasks). ⚠️ **Wer hier „wieder senden lässt", erzeugt Doppel-Antworten; wer den Rückgabewert ignoriert, verschluckt die Antwort still** (genau so beim Umbau am Autorun-Pfad passiert) | `grep -c "await stream_response(" bot.py` = **3**, und jede Stelle sendet danach via `send_answer_to_user` bzw. reicht den Text an `_run_job` zurück |
| **`send_answer_to_user()`** (zentraler Sendepfad) | Antwort-Ausgabe inkl. TTS-Chunking; **Vorstufe von 5.8** — künftige Sende-Stellen hier andocken, nicht daneben | Antwort erscheint in Telegram (Text bzw. Sprachnachricht bei TTS an) |
| **`_MODEL_ALIASES` + Tastatur Layout Y** (Fable-Button, 22.07.) | Modell-Wahl (Buttons + Prefs), `_model_btn_label` (EINE Label-Stelle für Gründlich-Text, Wechsel-Bestätigung, `/status`), 🎯 Gründlich (nutzt aktives Modell, KEIN `model_override` mehr), `/hilfe`-Text (8.6) | Trockentest: `_main_keyboard` liefert 4+3+2 Buttons; `_MODEL_ALIASES["fable"]` = `claude-fable-5`; ⚠️ ein neues Modell braucht IMMER: Alias + beide Button-Konstanten + `_ALL_KEYBOARD_BTNS` + `_MODEL_IDS` + `_model_btn_label` + `/hilfe` im selben Commit |
| **`_WHISPER_SEM`** (transcribe.py, Serialisierung, 22.07.) | Voice-Kette unter Last: verhindert parallele whisper-Prozesse (Ressourcen-Kollaps 22.07.); Tempo-Ausbau faster-whisper (5.22) muss die Sperre ÜBERNEHMEN, sonst kehrt die Fehlerklasse zurück | `grep -A2 "async with _WHISPER_SEM" transcribe.py` zeigt den whisper-Aufruf innerhalb der Sperre; ffmpeg-Konvertierung steht AUSSERHALB |
| **`_download_tg_file` Namens-Eindeutigkeit** (`{ts}-{uuid6}_{name}`, 22.07.) | Voice-Guard (`audio_path` in 5.2-Records), Transkription (WAV-Pfad wird vom Download-Namen abgeleitet!), Upload-Verarbeitung aller Dateitypen | Zwei sekundengleiche Downloads ergeben verschiedene Pfade; ⚠️ wer das Namensschema ändert, ändert auch die WAV-Ableitung `src.with_suffix(".wav")` mit |
| **Zustellnachweis (`send_answer_to_user -> bool`) + Text-Fallback bei TTS-Ausfall** `[NEU 19.07.]` | `_run_job` entscheidet **daran**, ob die Nachricht als beantwortet gilt (`False` → Ausgang „fehler", Record bleibt liegen). ⚠️ **Live-Beleg 19.07.:** edge-tts war kurz nicht erreichbar; `_send_tts_chunk` gab `None` zurück, der Aufrufer prüfte das nicht → Antwort erzeugt, **nie zugestellt**, trotzdem als erledigt abgehakt und der Persistenz-Record gelöscht. Wer den Rückgabewert wieder ignoriert oder den Fallback entfernt, stellt genau diesen stillen Antwortverlust wieder her | Selbstcheck-Zeile „Zustellnachweis + TTS-Fallback" grün (prüft Rückgabetyp **und** das Vorhandensein des Fallbacks); im Log erscheint bei TTS-Ausfall `TTS-Chunk fehlgeschlagen — Text-Fallback` statt Stille |
| **`pending.py` + `logs/pending/<key>.json`** (5.2 Nachrichten-Persistenz, seit 17.07.) | `process_user_text` (`record` beim Empfang), `_session_worker` (Status/`resolve` je nach **`_run_job`-Ausgang**: `beantwortet`/`aufgegeben`→löschen, `offen`→bleibt=Kontext-Retry, `fehler`→bleibt liegen), `_reconcile_pending` (Start), `run_self_check`, `/status`. ⚠️ Wer die vier Rückgabe-Strings von `_run_job` umbenennt/streicht, bricht die Status-Pflege **still** (Records würden nie gelöscht → der Reconcile griffe erledigte Nachrichten erneut auf) | Selbstcheck-Zeilen „Nachrichten-Persistenz (5.2)" **und** „Wiederaufgreif-Pfad (5.2)" grün; `python3 -c "import pending"` fehlerfrei; `logs/pending/` schreibbar (record→resolve hinterlässt keine Datei) |
| **`QueuedJob`-Primitive statt `update`** (5.2 Schritt 2, seit 19.07.) | Der **gesamte** Job-Pfad: `_run_job` und `_presend_gate` lesen `job.user_id/chat_id/message_id/thread_id/message_date` — **nicht** `job.update.*`. Bei nachgeholten Jobs ist `update` **None** (ein `telegram.Update` überlebt keinen Neustart). ⚠️ Wer irgendwo wieder `job.update.…` einbaut, bricht ausschließlich den Wiederaufgreif-Pfad — im Alltag fällt das **nie** auf, erst beim nächsten harten Neustart geht die Nachricht verloren | `grep -n "job\.update" bot.py` liefert **nur** den abgesicherten Bot-Fallback in `_run_job` (`if job.update is not None`); Selbstcheck „Wiederaufgreif-Pfad (5.2)" baut einen Job mit `update=None` |
| **Crash-Restart-Grund (`_write_crash_restart_reason`) + Secret-Maske (`_mask_secrets`)** `[NEU 20.07.]` | Der `try/except BaseException` um `main()` am Dateiende — **ohne ihn stirbt der Bot wortlos** und meldet nach dem Neustart nur „Bin wieder da", ohne dass Adam vom Absturz erfährt. ⚠️ **`_mask_secrets` ist Pflicht auf jedem Fehlertext, der in Datei/Chat landet:** Telegram-Fehler zitieren die API-URL **inklusive Bot-Token** (`InvalidToken` sogar wörtlich) — ungefiltert stünde der Token in `bot-restart-reason.txt` (wird gebackupt!), im Chat-Log und in der Startnachricht. Beim Testcrash am 20.07. genau so aufgetreten und behoben | Testcrash mit ungültigem Token (lokal, `HOME` umbiegen!): Datei wird geschrieben, enthält **keinen** Token; `grep -c "_mask_secrets" bot.py` ≥ 2 |
| **`_reconcile_pending` ist die EINZIGE Instanz, die unbeantwortete Nachrichten aufgreift** `[NEU 20.07.]` | Niemand sonst darf raten, was noch offen ist. `_detect_pending_item` **Fall B** tat genau das (Heuristik „wer war zuletzt im Chat-Log dran?") und beantwortete am 20.07. eine längst beantwortete Frage ein zweites Mal — die Session-Grenze nach dem Kill-Test hatte die Heuristik getäuscht. **Fall B ist abgeschaltet**; Fall A (Claude wartet auf Adams Antwort) bleibt, er deckt einen anderen Zweck ab. ⚠️ Wer einen zweiten Nachhol-Weg einbaut (Log-Analyse, Memory-Eintrag, Autorun), bricht die Zusage „jede Nachricht genau einmal" | `grep -n "Fall B" bot.py` zeigt nur den Abschalt-Kommentar; nach einem Neustart ohne liegende Records wird **keine** alte Nachricht erneut beantwortet |
| **Dedup-Sperre `_RESUMED_KEYS`** (5.2 Schritt 2) | `process_user_text` — verwirft die **Zweitzustellung** einer Nachricht, die der Startup-Reconcile bereits nachgeholt hat. Nötig, weil `DROP_PENDING_UPDATES=False` (gewollt!) Telegram dieselben Nachrichten nach einem Kill erneut liefern lässt. Ohne die Sperre: **doppelte Antwort** auf dieselbe Frage | Kill-Test (5.2): jede Nachricht wird nach dem Neustart **genau einmal** beantwortet; Log zeigt ggf. `dedup: … Telegram-Zweitzustellung verworfen` |
| **Schleifen-Bremse `_MAX_RESUME_ATTEMPTS`** (Default 3) | `_reconcile_pending` — begrenzt, wie oft eine liegengebliebene Nachricht automatisch nachgeholt wird. ⚠️ Auf 0 gesetzt oder entfernt: Eine Nachricht, die den Bot reproduzierbar mitreißt, wird bei **jedem** Start erneut nachgeholt → Neustart-Schleife | Selbstcheck „Wiederaufgreif-Pfad (5.2)" prüft `>= 1` und den Versuchszähler (`pending.bump_attempts`) |
| **`presend.py`** + `presend.WEEKDAYS_DE` | Pre-Send-Hook (8.5) im `_run_job`/Autorun. **`WEEKDAYS_DE` ist die EINZIGE Wochentagsliste** — `_current_datetime_context()` referenziert sie; zweite Liste anlegen ⇒ stiller Drift zwischen Prompt und Prüfung | `python3 -c "import presend; print(presend.WEEKDAYS_DE)"`; `grep -c "weekdays_de = \[" bot.py` = **0** (keine Zweitliste!) |
| **Echte Sendezeit → `QueuedJob.message_date`** (seit 19.07. Primitiv statt `update.message.date`) | Ehrliche Zeitzeile im Prompt (`_current_datetime_context(received_dt)`) + konsistente Referenz für den Hook. Fällt sie weg → Prompt behauptet wieder eine falsche Eingangszeit. **Umgestellt mit 5.2 Schritt 2:** die Zeit wird beim Empfang als Unix-Zeit **persistiert**, damit auch eine nach Neustart nachgeholte Nachricht ihre echte Sendezeit behält (ein lebendes `update` gibt es dort nicht mehr). ⚠️ Nicht mit `received_at` verwechseln — das ist die Verarbeitungszeit und liegt bei nachgeholten Nachrichten deutlich später | `process_user_text` füllt `message_date` aus `msg.date.timestamp()` und schreibt es in den Record; `_run_job` baut `received_dt` daraus; bei Wartezeit >2 Min nennt die Zeile Eingang UND Jetzt |
| **Session-Wächter `stall_watchdog` + `UserSession.last_activity`** (5.18) `[NEU 20.07.]` | Der einzige Schutz gegen „Bot lebt, Claude-Session tot" (live 23.06.: Bot nahm Nachrichten an, quittierte sie, antwortete nie — **niemand merkte es**, weil kein Fehler anfällt). Drei Teile, die nur GEMEINSAM wirken: (1) `stream_response` setzt `sess.last_activity` bei **jeder** SDK-Nachricht, (2) `stall_watchdog` vergleicht gegen `max(mb.current_started, sess.last_activity)`, (3) `post_init` startet die Schleife. ⚠️ **Fällt ein Teil weg, ist der Wächter still weg** — dieselbe Klasse Fehler, gegen die er schützt. ⚠️ Der Abbruch läuft **bewusst ohne `sess.lock`** (den hält der hängende Vorgang); wer dort eine Sperre einbaut, legt den Wächter lahm. ⚠️ Sessions mit offener Permission-Anfrage werden **übersprungen** — sonst zieht der Wächter Adam die Sitzung weg, während er über einen Freigabe-Button nachdenkt | Selbstcheck-Zeile „Session-Wächter (5.18)" grün (prüft alle drei Teile + Lockfreiheit); Verhaltenstest `.venv/bin/python scripts/test_stall_5_18.py` endet mit „ALLE TEILPRÜFUNGEN BESTANDEN"; Log beim Start: `Stall-Wächter gestartet (Intervall=…)` |
| **Stall-Stellschrauben `STALL_LIMIT` / `STALL_CHECK_INTERVAL` / `MAX_STALL_RETRIES`** (5.18) `[NEU 20.07.]` | Abwägung Fehlalarm ↔ Erkennungszeit. `STALL_LIMIT` zu klein ⇒ der Wächter killt **arbeitende** Sessions (kostet Abo-Kontingent, weil die Nachricht neu gestellt wird); zu groß ⇒ Adam wartet unnötig lange. `MAX_STALL_RETRIES` (Default 1) verhindert die Endlosschleife „hängt → neu → hängt" bei einer Nachricht, die den Hänger auslöst | Selbstcheck prüft `STALL_LIMIT_S >= 60`; `/status` zeigt bei stiller Session „letzte Regung vor …s (Wächter greift ab …s)" |
| **Voice-Eingangsschutz `VOICE_STAGE` + `pending.merge()`** (5.2) `[NEU 20.07.]` | Schließt das **25-Sekunden-Loch**, in dem eine Sprachnachricht durch nichts geschützt war (Persistenz entstand erst nach Download **und** Transkription). Zwei Teile, die zusammengehören: (1) `on_voice` legt den Eintrag **vor** dem Download an, (2) `_reconcile_pending` erkennt die Stufenmarke und **meldet statt nachzuholen**. ⚠️ **Reihenfolge ist die Funktion:** rutscht `pending.record` hinter `_download_tg_file`, ist die Lücke wieder offen — der Selbstcheck vergleicht deshalb die Position im Quelltext. ⚠️ Der Platzhaltertext darf **niemals** an Claude gehen; fiele die `VOICE_STAGE`-Abfrage im Reconcile weg, bekäme das Modell eine leere Hülle statt Adams Anliegen vorgelegt. ⚠️ Jeder Abbruchzweig in `on_voice` **muss** `_resolve_voice_stage` aufrufen, sonst meldet der Bot dieselbe Sprachnachricht bei jedem künftigen Start erneut | Selbstcheck-Zeile „Voice-Eingangsschutz (5.2)" grün (prüft Reihenfolge, Abbruchzweige, Reconcile-Erkennung, Vorhandensein von `pending.merge`); Verhaltenstest `.venv/bin/python scripts/test_voice_entry_guard.py` endet mit „ALLE TEILPRÜFUNGEN BESTANDEN"; Log bei jeder Sprachnachricht: `Sprachnachricht empfangen: … Eingang gesichert` |
| **Medien-Transportkette H1** (25.07.): `media.py` (ffmpeg/ffprobe) + `SDK_MAX_BUFFER`/`MEDIA_BUDGET` in bot.py + `max_buffer_size` in BEIDEN `ClaudeAgentOptions` + `is_transport_overflow()`-Zweig in `_run_job` + `on_photo`/`on_video` | Bild- und Video-Verarbeitung überhaupt. ⚠️ **Drei Glieder, die zusammen die Funktion sind:** (1) der erhöhte Puffer — wer `max_buffer_size` an einer der beiden Options-Stellen vergisst, bekommt den 1-MB-Abbruch dort zurück; (2) `MEDIA_BUDGET` ist vom Puffer ABGELEITET, nie eine feste Zahl — wer den Puffer ändert, ändert das Budget automatisch mit, wer das Budget hart verdrahtet, bricht die Kopplung; (3) **ffmpeg/ffprobe sind Systemvoraussetzung** — fehlen sie, bleiben große Bilder und alle Videos liegen. Das Original wird nie überschrieben: die Transportfassung ist immer eine ZUSÄTZLICHE Datei | `.venv/bin/python scripts/test_media_h1.py` → „Alle H1-Medientests bestanden"; `which ffmpeg ffprobe` liefert beide; Selbstcheck-Zeile „Medien-Transport (H1)" grün |
| **Start-Wächter B1** (25.07.): `scripts/start_waechter.py` + `_waechter_scharf()` im Updater + Freeze-Ablage `~/.claude/updater/freeze_vor_neustart.txt` + Postfach-Ausgang | Der Fall, den die Updater-Kette NICHT abdeckt: der Bot kommt nach dem Neustart nicht mehr hoch — ein Rollback von innen bräuchte einen laufenden Prozess. Voraussetzung für B2/B3. ⚠️ **Er muss abgekoppelt starten** (`start_new_session=True`): läuft er als Kind des Bots, stirbt er mit ihm und ist genau dann weg, wenn er gebraucht wird. ⚠️ Er meldet über das **Postfach**, nicht direkt — den Token hat nur der Bot; die Meldung erreicht Adam also erst nach der Genesung, deshalb zusätzlich die Zustandsdatei `startwaechter.json` für den 4-Uhr-Check. ⚠️ Sein Neustart-Weg ist **SIGTERM + systemd `Restart=always`** — wer `Restart=` in der Unit ändert, nimmt dem Wächter die Wiederbelebung | `.venv/bin/python scripts/test_start_waechter_b1.py` → „Alle B1-Start-Wächter-Tests bestanden“; `systemctl show claude-telegram-bot -p Restart` = `Restart=always` |
| **Datenschutz-Ampel `ampel.py`** (2.2, Beobachtungsphase) | Einstufung jeder eingehenden Nachricht (grün/gelb/rot) + Protokoll als Grundlage der späteren Umleitung roter Inhalte auf lokale Verarbeitung. ⚠️ Sie ist heute **beobachtend, nicht durchsetzend** — wer sie für einen Schutz hält, überschätzt sie; das Enforcement kommt erst nach der Auswertung. ⚠️ `ampel.observe()` wird in `process_user_text` bewusst **fehlertolerant** aufgerufen: Ein Fehler in der Einstufung darf nie eine Nachricht verschlucken. Die Regelpflege der heikelsten Muster läuft über den cloud-freien Weg (`/ampel`-Knöpfe bzw. Textbefehl), nicht über den Agenten | `.venv/bin/python -c "import ampel; print(ampel.observe('Test'))"` liefert eine Einstufung ohne Ausnahme; Selbstcheck-Zeile „Register-Vollständigkeit (R2)" grün |
| **Nachzieher-Kette C1** (25.07.): `scripts/nachzieher.py` + `_folge_patch()` im Updater + Ablage `~/postfach/nachzieher/` | Schließt die Lücke zwischen eingespielter Fassung und gepinnter Fassung — ohne sie zeigt `requirements.txt` weiter auf die alte Version und der nächste Wiederaufbau fällt still zurück (genau die Divergenz, die C2 meldet). ⚠️ **Die beiden Weißlisten SIND die Sicherheit**, nicht ein Zusatz: `ERLAUBTE_DATEIEN` (nur requirements.txt/components.json) und die Muster für Paketname/Version. Wer eine davon aufweicht — etwa um „auch mal bot.py" zu erlauben —, macht aus dem Nachzieher einen Schreibweg für den einspritzungsexponierten Bot-Prozess und kehrt 8.7 um. ⚠️ Der Patch trägt **keinen Fließtext**; das `grund`-Feld wird nie ausgeführt und ist auf harmlose Zeichen begrenzt. ⚠️ Er läuft **außerhalb** des Bots und legt neue Pins **nie** an, er ersetzt nur vorhandene | `.venv/bin/python scripts/test_nachzieher_c1.py` → „Alle C1-Nachzieher-Tests bestanden" (11 Prüfungen, davon 9 Ablehnungen) |
| **Sprach-Backend faster-whisper** (25.07., ersetzt whisper.cpp als Vorgabe): `FasterWhisperTranscriber` in `transcribe.py` + pip-Paket `faster-whisper` (CTranslate2) + Modell-Zwischenlager `~/.cache/huggingface` | Die gesamte Voice-Kette. ⚠️ **Die Sperre `_WHISPER_SEM` MUSSTE mitwandern** — das Register hatte genau das vorab verlangt; ohne sie kehrt der Ressourcen-Kollaps vom 22.07. zurück. ⚠️ Der **Rückweg** ist eine Umgebungsvariable (`STT_BACKEND=whisper_cpp`), und ein Fehlschlag beim Laden fällt **laut** auf whisper.cpp zurück (Log + eigene Selbstcheck-Zeile) — ein stiller Rückfall wäre schlimmer als ein Ausfall. ⚠️ Das Modell lädt beim **ersten** Aufruf (~1,4 s) und bleibt im Speicher; wer die Instanz je Anfrage neu baut, verliert den Tempogewinn. ⚠️ Der Modell-Umschalter (5.22) übergibt historisch **Pfade** — `set_model` liest die Stufe aus dem Dateinamen, damit der Knopf ohne Oberflächen-Umbau weiterläuft | Selbstcheck-Zeile „Sprach-Backend (5.22)" grün (nennt das AKTIVE Backend, nicht das gewünschte); gemessen 25.07. auf dem VPS: 535,9-s-Voice in **80,3 s** statt 145,7 s (1,81×) |
| **Kalender-/Erinnerungs-Kette (CalDAV)** (25.07., vorgezogen aus Phase 7): `kalender.py` + pip-Paket `caldav` + Befehle `/termine`, `/aufgaben` + Umgebungsvariablen `ICLOUD_CALDAV_USER`, `ICLOUD_CALDAV_APP_PASSWORT` | Die häufigste Alltagsbitte überhaupt (Prüfraster-Befund). Kalender UND Erinnerungen laufen über dasselbe Verfahren — ein Bau statt zwei. ⚠️ **Ohne Zugangsdaten arbeitet das Modul GAR NICHT und sagt es deutlich** (`NichtEingerichtet`); ein halb verbundener Kalender wäre schlimmer als keiner, weil man ihm glaubt. ⚠️ Es braucht ein **anwendungsspezifisches Kennwort** von `appleid.apple.com` — das normale Apple-Kennwort scheitert bei CalDAV. ⚠️ Die Zugangsdaten sind **Geheimnisse**: nur Umgebung, niemals Repo; `_is_sensitive_ref` deckt sie über das Muster `passwort` mit ab. ⚠️ Eine unbekannte Sammlung wird **benannt, nicht ersetzt** — in den falschen Kalender zu schreiben ist schlimmer als nicht zu schreiben. ⚠️ Der AppleScript-Weg des Mac existiert auf Linux nicht; deshalb CalDAV | `.venv/bin/python scripts/test_kalender_caldav.py` → „Alle Kalender-Tests bestanden" (läuft ohne Netz und ohne Zugangsdaten); live nach Einrichtung: `/termine` zeigt echte Einträge |
| **Log-Sync-Filter für Ausarbeitungen** (25.07.): `rsync`-Regelblock in `scripts/log_sync.sh` + Unterordner `ausarbeitungen/` im Log-Repo | Der Weg von Claudias erarbeiteten Dokumenten aus dem Arbeitsordner zu Adam und Conni. ⚠️ **Die REIHENFOLGE der rsync-Regeln IST die Funktion:** rsync nimmt die **erste** zutreffende Regel — Ausschlüsse müssen **vor** den Einschlüssen stehen. Standen sie danach, gewann `--include='*.md'`, und Ausgeschlossenes wanderte doch mit. **Genau so ist am 25.07. die Kontext-Datei `CLAUDE.md` (146 KB Sitzungskontext) ins Log-Repo gelangt**, obwohl sie ausdrücklich ausgeschlossen war. Wer diesen Block umsortiert, öffnet den Weg erneut. ⚠️ Zusätzlich ein **Nachweis statt Vertrauen**: `rm -f` auf die beiden Kontext-Dateien nach dem Lauf | `bash scripts/log_sync.sh` und danach `ls ~/logsync/claude-bot-logs/ausarbeitungen \| grep -c CLAUDE` = `0`; im Zielordner liegen nur Dokument-Endungen |
| **Wartungsfenster B2/B3** (25.07.): `scripts/wartungsfenster.py` + Zustandsdateien `vorgemerkt.json`, `probelauf.json`, Marke `scharf` + Knöpfe „🌙 04:00" und „🗑 Storno" in `/updates` + Kopplung an `claude-bot-hygiene.timer` (04:00) | Nimmt Adam den **Neustart** nach gelben/roten Updates ab — nicht die Entscheidung. ⚠️ **Es setzt B1 voraus**: Ohne Start-Wächter gäbe es niemanden, der einen misslungenen Hochlauf nachts um vier zurückrollt. ⚠️ **Die Marke `scharf` ist die einzige Schranke** zwischen Melden und Handeln; sie wird **nie vom Fenster selbst** gesetzt — wer das ändert, nimmt Adam aus der Schleife. ⚠️ Zur Ausführungszeit wird die Fassung **nachgeprüft** (wie A3); eine abweichende wird nicht eingespielt, sondern neu gefragt. ⚠️ Der Lauf ist an den **bestehenden** 04:00-Neustart gekoppelt — ein eigener Neustart wäre eine zweite Unterbrechung ohne Gewinn; wer den Hygiene-Timer entfernt, muss den Fenster-Aufruf mitnehmen | `.venv/bin/python scripts/test_wartungsfenster_b3.py` → „Alle B2/B3-Wartungsfenster-Tests bestanden" (8 Prüfungen); live: `/updates` zeigt „🌙 04:00"-Knopf, Vorgemerktes und Storno |
| **Große-Dateien-Kette 5.34** (25.07., Bot-Seite gebaut): `TELEGRAM_API_BASE` + `LOKALER_API_SERVER`/`DATEI_GRENZE` + `base_url`/`base_file_url`/`local_mode` im Builder + `scripts/api_cache_pflege.sh` + Deckel-Prüfung in `daily_check.sh` + Geheimnis-Marker `/etc/telegram-bot-api` | Empfang großer Videos/Fotos/Dateien (2 GB statt 20 MB). ⚠️ **BEIDE Adressen umstellen** — `base_url` UND `base_file_url`; die zweite wird gern vergessen, und ohne sie liefen die Downloads weiter über Telegram, also weiter mit 20 MB (dieselbe Klasse wie die zwei Options-Stellen bei H1). ⚠️ `DATEI_GRENZE` ist vom aktiven Weg **abgeleitet**, keine feste Zahl — wer sie hart schreibt, bricht den Rückweg. ⚠️ Der Token lebt dann an einer **zweiten Stelle**: `/etc/telegram-bot-api*` ist deshalb **vor** dem Bau in den Geheimnis-Markern. ⚠️ **Das Zwischenlager gehört NICHT ins Backup** — `vps_backup.sh` sichert eine Pfadliste, nicht den Baum; wer die Liste erweitert, kippt 4.1 (zweistellige Gigabyte). ⚠️ Ohne die Aufräum-Pflege füllt sich die Platte still | Selbstcheck-Zeile „Große Dateien (5.34)" grün (prüft Geheimnis-Marker, Aufräum-Skript, Deckel-Prüfer und die Ableitung der Grenze); `bash scripts/api_cache_pflege.sh` liefert den Füllstand; ausgeschaltet meldet der 4-Uhr-Check „kein eigener Bot-API-Server aktiv" |
| **Link-Inbox 5.14** (25.07.): `linkinbox.py` + Ablage `~/.claude/link-inbox/inbox.json` + `_text_ohne_links()` in bot.py + Knöpfe `lnk:*` + `/links` | Links werden abgelegt statt sofort verarbeitet — kein Kontingent für etwas, das Adam nur parken wollte. ⚠️ **`_text_ohne_links` ist die Weiche:** Sie entscheidet zwischen „Ablage" und „Auftrag". Zu großzügig → Adam verliert Sätze; zu eng → jeder Link löst doch einen Lauf aus. Satz- und Sonderzeichen zählen ausdrücklich **nicht** als Text (ein vorangestellter Pfeil hat den Test beim ersten Lauf gekippt). ⚠️ Das Modul greift **nie aufs Netz** — ein eigener Test sucht nach Netz-Bibliotheken im Quelltext; abgerufen wird erst auf Knopfdruck. ⚠️ Der Titel ist aus der Adresse **abgeleitet, nicht gelesen** — das wird in der Nachricht gesagt, damit er nicht wie ein gelesener wirkt (Beleg-Grundsatz) | `.venv/bin/python scripts/test_linkinbox_5_14.py` → „Alle 5.14-Link-Inbox-Tests bestanden" (8 Prüfungen); live: nackter Link → Ablage mit drei Knöpfen, `/links` zeigt die Ablage |
| **Freigabe-Postfach 9.4** (25.07.): `freigaben.py` + `freigabe_worker`/`on_freigabe_callback` in bot.py + `/freigaben` + `scripts/entscheidungs_protokoll.py` + Ordner `~/postfach/freigaben/{anfragen,urteile,protokoll}` | **Die fehlende Leitung** zwischen Bot-Chat und Ablage — und die Voraussetzung für Hora, das keine Entscheidungen treffen darf, sondern parken muss. ⚠️ **Die Leitplanken sind der Bau, nicht die Verzierung:** Geheimnis-Marker sind in `freigaben.py` **gespiegelt**, weil das Postfach auch von Prozessen befüllt wird, die `bot.py` nicht kennen — wer sie dort entfernt, öffnet den Kanal für Geheimnisse. Die Kennungsprüfung im Callback geht gegen die **Allowlist**, nicht gegen den Chat (eine weitergeleitete Nachricht darf niemanden ermächtigen). **[KORRIGIERT 25.07.] Die Frist ist eine Auffrischung, kein Verfall:** `auffrischen()` legt fällige Anfragen **erneut vor** (24 h), statt sie abzulehnen — Schweigen darf nie bewirken, dass etwas passiert, heißt aber auch nicht Nein. Wer daraus wieder eine Ablehnung macht, erzeugt **erfundene Urteile im Entscheidungs-Protokoll**. Ins Protokoll kommt **nur ein tatsächliches Urteil**; Unbeantwortetes führt `unbeantwortet()` in einer eigenen Liste. Grundlage der Unterscheidung „nicht da gewesen" / „gesehen, offen" ist `bot.letzte_regung()` — gespeist von **einem** `TypeHandler` in Gruppe −1; eine Spur, die an dreißig Stellen gepflegt werden müsste, wäre an der einunddreißigsten vergessen. ⚠️ Der Bot schreibt das Protokoll **nicht** ins Repo (8.7): `entscheidungs_protokoll.py` läuft außerhalb, hängt **nur Tabellenzeilen an** und säubert Umbrüche und senkrechte Striche — ein untergeschobenes Urteil kann so höchstens eine Zeile erzeugen, nie eine Regel ändern. ⚠️ **[25.07.] Die Einfügestelle wird gesucht, nicht unterstellt** (`_einfuegen`): Zeilen gehen vor die **nächste Überschrift** nach dem Protokoll-Abschnitt, nicht ans Dateiende. Vorher stand dort die Annahme „der Abschnitt steht am Dateiende" — landet je ein Abschnitt danach, wandern Zeilen still in den falschen, und ein Protokoll, dessen Zeilen anderswo auftauchen, ist schlimmer als keines | `.venv/bin/python scripts/test_freigaben_9_4.py` → „Alle 9.4-Freigabe-Tests bestanden" (9 Prüfungen, 4 davon Abweisungen); Durchstich geprüft: Anfrage → Urteil → datierte Zeile im Drehbuch |
| **Hora — autonomer Läufer** (25.07.): `scripts/hora.py` + Auftragsliste `~/.claude/hora/auftragsliste.json` + Fehlserien-Zähler + Postfach-Bericht; **setzt 9.4 voraus** | Arbeitet während Adams Abwesenheit die **vorab freigegebene** Liste ab. ⚠️ **Er darf nichts entscheiden** — Zustimmungspflichtiges wird über 9.4 **geparkt**; wer diesen Zweig entfernt, macht aus einem Läufer einen Entscheider. ⚠️ **Auf rotem Fundament wird nicht gearbeitet** (dieselbe Regel wie A5 im Updater) — sonst ist die Ursache später nicht mehr zuzuordnen. ⚠️ **Ein Fehlschlag hakt nichts ab**; nach drei Fehlläufen in Folge hält er an, bis die Datei `fehlserie.json` entfernt wird. ⚠️ Er führt **nur aus, was in der Liste steht** — ohne hinterlegten Befehl wird nichts geraten. ⚠️ Bewusst **ohne Kenntnis der Kontingent-Grenzen** gebaut (sie sind von hier aus nicht messbar). **[KORRIGIERT 25.07.] Er arbeitet VERKETTET** — Auftrag um Auftrag, frische Sitzung je Auftrag, bis die Liste leer ist, das Kontingent anschlägt (`_kontingent_erschoepft`, hält an **ohne abzuhaken**) oder die Fehlserie zuschlägt. „Einer je Lauf" war selbst eine Annahme, nämlich die, dass das Kontingent knapp ist. ⚠️ **Eine Frage hält ihn NICHT an:** Zustimmungspflichtiges wird geparkt, gemeldet, und er geht sofort zum nächsten **unabhängigen** Auftrag. Was unabhängig ist, sagt `haengt_an` in der Liste — fehlt die Angabe, gilt ein Auftrag als unabhängig; wer `_blockiert` entfernt, lässt Aufträge auf halbfertigen Vorgängern aufbauen | `.venv/bin/python scripts/test_hora.py` → „Alle Hora-Tests bestanden" (11 Prüfungen); Probelauf: `python3 scripts/hora.py --trocken` meldet, was er getan hätte |
| **Schnappschuss auf dem Server** (26.07.): `scripts/vps_schnappschuss.sh` (ROLLE: `vps-schnappschuss`) + Zeitgeber `claude-schnappschuss.timer` (03:30) | **Deckt die Luecke, die 4.1 offen laesst:** Die Mac-Sicherung zieht vom Server — schlaeft der Mac, holt launchd den Lauf erst NACH dem Aufwachen nach, in vierzehn Tagen also gar nicht. Betroffen waeren die Dateien, die NUR dort liegen (Gedaechtnis, Adams Offen-Liste, Ampel-Regeln). ⚠️ **Er ersetzt 4.1 NICHT** — er liegt auf derselben Maschine und hilft gegen Ueberschreiben, nicht gegen den Verlust des Servers. Wer ihn fuer ein Backup haelt, ueberschaetzt ihn. ⚠️ **Nichts verlaesst die Maschine** (Klienten-Namen im Gedaechtnis); Verzeichnis `700`, Archive `600`. ⚠️ Erst den neuen Stand schreiben, DANN alte abraeumen — andersherum staende man nach einem Fehlschlag ohne alles da | `bash scripts/vps_schnappschuss.sh` meldet Groesse und Anzahl; `ls -l ~/schnappschuesse/` zeigt `-rw-------` |
| **Horas Fehlgrund nennt die ROTE Zeile** (28.07., Halt 16:15): `_ROT_ZEILE` + `_fehlgrund` in `scripts/hora.py`; Prüfer `scripts/test_hora.py` | **Die letzte Zeile ist eine Positionsannahme, kein Inhaltsmerkmal.** Der Selbstcheck gibt 29 Zeilen aus, eine davon rot — und die stand mittendrin. Gemeldet wurde die letzte, eine grüne: „der Befehl meldete: ✓ Medien-Eingangsschutz". Korrekt und vollkommen nutzlos. ⚠️ **In Adams Abwesenheit hätte das niemand auflösen können** — jede Diagnose hätte eine Hand gebraucht, die nicht da ist. ⚠️ Höchstens drei rote Zeilen, und die Kürzung wird **genannt** — eine Meldung mit dreißig Zeilen wird nicht gelesen. ⚠️ **`_ROT_ZEILE` ist bewusst breit** (✗, ❌, Traceback, Error, not found …): Ein enges Muster fände genau die Ausgabe nicht, die neu ist. ⚠️ **Der Regressionsstand bleibt dem ANDEREN Fall vorbehalten** (Befehl lief, hat aber etwas beschädigt) — der Fund vom 26.07. darf nicht zurückfallen | `.venv/bin/python scripts/test_hora.py` → darunter „Fehlgrund nennt die ROTE Zeile, nicht die letzte" |
| **Auftragsbuch B8 Stufe 1 — GEBAUT UND RUHEND** (28.07.): `auftragsbuch.py` (ROLLE: `auftragsbuch`) mit `GRUENE_ARTEN`, `ROTE_WORTE`, `einstufen`, `legen`, `uebernehmen`; Prüfer `scripts/test_auftragsbuch_b8.py` | **Die Gegenrichtung zum Botenpostfach**: Dort geht etwas zu Adam hinaus, hier kommt etwas ins System hinein — damit Sitzungen einander Aufträge geben können, ohne dass Adam sie trägt (heute vier Wege je Auftrag, alle von Hand). ⚠️ **`SCHARF = False` — `uebernehmen()` übergibt NICHTS an Hora**, und sagt das auch: Ein Übergang, der leise nichts tut, sieht von außen aus wie einer, der leise alles tut. Scharfstellen erst nach der Gegenprüfung (CLAUDE.md ①a). ⚠️ **Grün kommt AUSSCHLIESSLICH aus der geschlossenen Liste `GRUENE_ARTEN`, nie aus einem Urteil im Einzelfall** — das Konzept benennt die Fehleinstufung selbst als gefährlichsten Fall, weil sie niemandem auffällt. Eine unbekannte Art ist gelb, auch wenn sie harmlos klingt. Jede Grün-Art trägt ihr Prüfdatum. ⚠️ **`ROTE_WORTE` haben bewusst KEINE schließende Wortgrenze** — der erste Entwurf hatte `\b…\b`, und die eigene Prüfung fiel sofort darüber: „Klient" schlug an, „Klientendaten" nicht, ausgerechnet beim heikelsten Wort. Deutsche Zusammensetzungen hängen ihr Bestimmungswort vorn an; eine Wortgrenze dahinter macht die Bremse für genau die Fälle blind, für die sie da ist. ⚠️ **Rot schlägt Grün** — ein Aufräum-Auftrag, der „Token" enthält, ist keiner mehr. ⚠️ Die Einstufung wird beim Ablegen **mitgeschrieben**, nicht bei jedem Lesen neu gerechnet: Ändert sich die Grün-Liste, bleibt nachvollziehbar, unter welcher Regel ein Auftrag hereinkam | `.venv/bin/python scripts/test_auftragsbuch_b8.py` → zehn Zeilen, darunter „NICHT scharfgestellt — und der Riegel sagt es" |
| **Blinde-Flecken-Verfahren** (28.07., B6): `docs/blinde-flecken-verfahren.md` (ROLLE: `blinde-flecken-verfahren`) + Wächter `scripts/test_blinde_flecken_b6.py` + die korrigierte Zeitgeber-Suche in `scripts/daily_check.sh` | **Drei Fragen an jeden Prüfer: Was prüft er NICHT · wer trägt ihn · steht seine Vorgabe im Text oder im Code.** Der Lieblingsfehler dieses Projekts hat immer dieselbe Signatur — ein Ausbleiben, das wie Ruhe aussieht. ⚠️ **Beim ersten Anwenden fand Frage ③ sofort einen Fall im eigenen Bestand:** Über der Zeitgeber-Suche stand „DIE ZEITGEBER WERDEN GESUCHT, NICHT AUFGEZÄHLT" — darunter ein Filter auf die Namensanfänge `claude-`, `hora`, `stundenblume`. Eine Positivliste in Verkleidung; ein neunter Zeitgeber mit anderem Namen wäre durchgefallen. ⚠️ **Das Suchmerkmal ist jetzt das ZIEL, nicht der Name:** Was `/home/claudebot` in seinem `ExecStart` trägt, ist unseres. Gemessen: trennt alle sieben eigenen von allen sechs System-Zeitgebern und überlebt jede Umbenennung. ⚠️ **Der Wächter kann Frage ③ nicht allgemein prüfen** — ob Kommentar und Code dasselbe sagen, beantwortet ein Leser. Er bewacht die konkreten Fallen, die wir kennen; ein Prüfer, der alles verspräche, wäre selbst der nächste blinde Fleck. ⚠️ **Gegenprobe belegt** (Rückfall auf die Positivliste nachgestellt → schlägt an) | `.venv/bin/python scripts/test_blinde_flecken_b6.py` → vier Zeilen, darunter „die Zeitgeber-Suche ist keine Positivliste" |
| **Kreuzverschränkung Blumen ↔ Tagescheck** (28.07., Connis Fund): `tagescheck_pruefen()` in `scripts/stundenblume.py` (Kennung `tagescheck-still`, 26 h) ↔ `stundenblume.py --pruefen` in `scripts/daily_check.sh` | **Was ein Prüfer trägt, kann er nicht prüfen.** Die Zeitgeber-Wache konnte jeden Zeitgeber prüfen — außer den, der sie selbst startet: Sie lebt in `daily_check.sh`, und der läuft über einen Zeitgeber. Stirbt ausgerechnet dieser, stirbt die Wache mit ihm, und niemand meldet es. ⚠️ **Die Lösung ist keine bessere Selbstprüfung, sondern eine zweite Instanz mit eigenem Antrieb** — die Blumen laufen über einen eigenen Zeitgeber. Wer eine der beiden Prüfrichtungen entfernt, macht aus der Verschränkung wieder einen Selbstbezug; ein eigener Prüfer verlangt deshalb **beide**. ⚠️ **Hier ist die feste Schwelle ausnahmsweise richtig** (26 h), weil der Takt bekannt und fest täglich ist — im Gegensatz zur Zeitgeber-Wache, die `NextElapseUSecRealtime` misst, weil ihr Takt unbekannt ist. Eine Regel ist nicht schlecht, weil sie eine Zahl enthält, sondern wenn die Zahl geraten ist. ⚠️ **25 Stunden alarmieren NICHT** — ein verspäteter Lauf (Neustart, Wartungsfenster, Zeitumstellung) darf nicht anschlagen, sonst ist der Wächter binnen zwei Tagen abgeschaltet | `.venv/bin/python scripts/test_stundenblumen.py` → darunter „die Verschränkung greift in BEIDE Richtungen" |
| **Vorlese-Regeln für Datum, Kennnummern, Jahre** (28.07., B5): `_normalize_dates`, `_normalize_kennnummern`, `_normalize_jahreszahlen`, `_strip_kontext_hinweis` — alle in der Kette von `_strip_markdown_for_tts`; Prüfer `scripts/test_vorlese_b5.py` | **Eine Ziffernfolge sagt nicht, was sie ist** — „2026" kann Jahr, Menge oder Kennungs-Ende sein. Deshalb entscheidet ausschließlich das Umfeld, und wo es nichts hergibt, wird NICHT geraten. ⚠️ **Die Reihenfolge in der Kette trägt die halbe Arbeit:** Bezugs-Vermerk zuerst raus · Datum VOR der Versionsregel (sonst wird `22.06.2026` zu „22 Punkt 06" und ist unrettbar) · Kennnummern VOR der Jahresregel (sonst wird eine Kennung in ihren letzten vier Ziffern zum Jahrhundert). Wer hier eine Zeile verschiebt, bricht etwas, das kein Syntaxfehler meldet. ⚠️ **Jede Regel braucht ein ankündigendes Wort** — Jahresform nur nach „seit/im/Jahr…", Ziffernweises nur nach „Nummer/IBAN/Beleg…" und erst ab fünf Ziffern. Ohne diese Bremsen würde „7940 MiB" zerhackt und „1985 Teilnehmer" zum Jahrhundert. ⚠️ **Ab 2000 wird nichts verändert** — dort ist die deutsche Jahresform mit der Zahlform identisch; jede Änderung wäre nur ein neues Risiko. ⚠️ **Geprüft wird der TEXT, nicht der Klang** — ob die Stimme „neunzehnhundertfünfundachtzig" sauber ausspricht, hört nur Adam | `.venv/bin/python scripts/test_vorlese_b5.py` → elf Zeilen, darunter „was KEIN Datum ist, bleibt unangetastet" |
| **Limit-Vorwarnung 5.20 + ehrlicher Verbrauch** (28.07., B4): `_RateLimitEvent` (weicher Import), `_limit_warnung_melden`, `_LIMIT_GEMELDET`, `_limit_zeitspanne`, `_record_usage` (Zwischenspeicher-Felder) + beide Anzeigestellen; Prüfer `scripts/test_limitwarnung_b4.py` | **Es wird nichts gerechnet, nur weitergereicht.** Ein eigener Token-Zähler kennt weder die Höhe des Kontingents noch, was Adams Desktop-Sitzungen und claude.ai auf dasselbe Konto buchen — er sähe aus wie eine Auskunft und wäre geraten (so 5.20 am 24.07. bewusst neu gefasst). ⚠️ **Der Anbieter schickt den Limit-Zustand mit JEDEM Lauf mit, nicht nur beim Umschlagen** — ohne `_LIMIT_GEMELDET`-Gedächtnis käme die Warnung mit jeder Antwort. Das ist derselbe Meldungssturm wie am 28.07. früh, nur an anderer Stelle. ⚠️ **Je Kontingent-Art ein eigener Eintrag**: Fünf-Stunden-Fenster und Wochenkontingent sind zwei Dinge; ein gemeinsames Gedächtnis verschluckte die zweite Warnung. ⚠️ **Der Import ist weich** (`except ImportError`) — ein harter hätte aus einem fehlenden Zusatzsignal einen Startabbruch gemacht: Der Bot bliebe stumm, weil eine WARNUNG fehlt. ⚠️ **GEMESSEN: Der Verbrauchszähler wies 63 Eingabe-Token je Antwort aus** (442 Antworten, 14 Tage) — `input_tokens` zählt nur den frisch übertragenen Teil, der Zwischenspeicher fehlte ganz; er wird jetzt getrennt geführt, damit ein Anstieg als teuer oder harmlos erkennbar bleibt. ⚠️ **`cost_usd` ist ein NENNWERT, kein abgebuchtes Geld** — über dieselben 14 Tage rund 3400 $, im Abo nicht berechnet; die Anzeige sagt das jetzt dazu | `.venv/bin/python scripts/test_limitwarnung_b4.py` → zehn Zeilen, darunter „Warnung kommt GENAU EINMAL je Zustand (Dämpfer)" |
| **`/updates` benennt, was es NICHT prüfen konnte** (28.07.): `scripts/updater.py` (`blinde_flecken()`, `classify` mit Art) + `cmd_updates` in bot.py + `braucht_root` in `components.json` | **Der Geschwisterpfad zum Monitor-Befund D — er stand noch offen.** `classify()` übersprang eine Komponente stillschweigend, sobald eine ihrer Auskünfte fehlte; Adam las „✅ Alles aktuell", während der Eintrag ungeprüft dastand. ⚠️ **Der Bot läuft als `claudebot` und ist NICHT in der Docker-Gruppe** (gemessen 28.07.) — das LobeChat-Abbild kann er nie selbst ansehen. Das ist eine bewusste Rechtegrenze, keine Störung: `braucht_root` sorgt dafür, dass sie als solche benannt wird und nicht wie ein Defekt klingt. ⚠️ **`classify` muss die Art an `_cmp` weitergeben** — ohne sie liefe ein Fingerabdruck durch den Zahlenvergleich, dessen Ergebnis an der zufälligen Ziffernlage der Hexadezimalfolge hinge | `.venv/bin/python scripts/test_version_monitor.py` → zehn Zeilen, darunter „/updates übergeht nichts stillschweigend (Geschwisterpfad)" |
| **Prüfarten und Fälligkeit im Versions-Monitor** (28.07., B2-Rest): `scripts/version_monitor.py` (`systempaket`, `docker`, `github_release`, `_faellig`) + `components.json` (`intervall_tage`) + `logs/version-monitor-gesehen.json` | **Von sechs ungeprüften Einträgen sind vier zu echten Prüfungen geworden — und die zwei verbliebenen melden sich jetzt überhaupt erst.** Vorher hingen `manual`-Einträge als Anhängsel an einer Meldung, die es nur gab, wenn ANDERSWO ein Update gefunden wurde: Lief alles rund, schwieg der Monitor auch über die Punkte, die **niemand sonst prüft**. ⚠️ **`docker` und `systempaket` vergleichen auf UNGLEICHHEIT, nicht auf Größe** — ein `:latest`-Abbild ändert seinen Inhalt ohne seinen Namen, und die führende `7` in `7:5.1.6-0+deb12u1` ist eine Epoche, keine Hauptversion. Ein Zahlenvergleich hätte hier still immer „aktuell“ gemeldet. ⚠️ **Beim ERSTEN Lauf ist nichts fällig** — sonst käme die ganze Liste auf einen Schlag; und nach jeder Meldung wird die Frist zurückgesetzt, weil eine wöchentlich wiederkehrende Erinnerung nicht gelesen, sondern abgeschaltet wird. ⚠️ **`claude-modelle`, `verfahren-medien` und `whisper.cpp` bleiben bewusst `manual`**: Die CLI führt keinen Modell-Katalog, „gibt es ein besseres Video-Verfahren“ ist ein Urteil, und der whisper.cpp-Klon steht (gemessen) auf einem Commit außerhalb jedes Release-Tags. Keine Attrappe an diesen drei Stellen. ⚠️ Alle Quellen anonym und kostenfrei (apt lokal, Docker-Registry und GitHub ohne Konto) — **es wird nur der Kopf des Manifests geholt, nie ein Abbild** | `.venv/bin/python scripts/test_version_monitor.py` → acht Zeilen, darunter „manual meldet sich nach Fristablauf VON SELBST“ |
| **Blinde Flecken des Versions-Monitors** (28.07., Befund A/D aus 5.21-E): `scripts/version_monitor.py` (Liste `blind`) + `components.json` + Prüfer `scripts/test_version_monitor.py` | **Was der Monitor NICHT prüft, meldet er jetzt.** Vorher wanderten eine unbekannte Art und eine weggebrochene Quelle nur ins Protokoll — und ein Protokoll, das niemand liest, ist kein Prüfer. Der Eintrag stand im Register, sah nach Abdeckung aus und wurde nie angesehen. ⚠️ **Wer eine neue Art in `components.json` einträgt, MUSS einen Handler in `HANDLERS` ergänzen** — sonst meldet der Monitor sie ab dem nächsten Lauf als blinden Fleck. Das ist Absicht: lieber eine Meldung zu viel als ein Eintrag, der ungeprüft dasteht. ⚠️ **Ein blinder Fleck ist auch dann meldepflichtig, wenn es sonst nichts gibt** — sonst wäre derselbe Fehler zweimal gebaut. ⚠️ Der Monitor **meldet, er handelt nicht** (E3): kein `pip install`, kein Modell-Aufruf (AGB, zeitgesteuert) — ein eigener Prüfer verbietet beides im Quelltext | `.venv/bin/python scripts/test_version_monitor.py` → fünf Zeilen, darunter „unbekannte Art wird GEMELDET, nicht übersprungen" |
| **Botenpost** (26.07.): `botenpost.py` (ROLLE: `botenpost`) — genutzt von `hora.py`, `stundenblume.py`, `wartungsfenster.py`, `start_waechter.py`; Gegenstück `_postfach_send_one` in bot.py; Prüfer `scripts/test_pruefumgebung.py` + `scripts/test_postfach_wiederaufgriff.py` | **EINE Stelle legt ab, und jede Nachricht nennt ihren Absender.** Vorher hatten vier Schreiber vier fast gleiche Codeblöcke — und keiner nannte sich. Als am 26.07. um 01:44 eine anonyme Meldung bei Adam ankam, kostete die Suche nach ihrem Urheber über eine Stunde; die Nachricht hätte es selbst sagen können. ⚠️ **Wer wieder selbst ablegt**, nimmt der Nachricht den Absender — der Prüfer verbietet es ausdrücklich. ⚠️ Der Absender steht **zweimal** drin, im Dateinamen **und** im Inhalt: Der Dateiname ist das, was man in einem vollen Ordner sieht, ohne eine Datei zu öffnen. ⚠️ **Feste Absenderliste, kein Freitext** — ein Absender, den sich jeder ausdenken kann, belegt nichts. ⚠️ Der Text wird vor dem Ablegen von Schlüssel- und Adressmustern befreit ⚠️ **A6.1 (20.08.): Der Tagescheck legt taeglich einen Sichtungs-Vermerk ins Auftragsbuch** — Adams Vorgabe, dass die Kontrolle einmal je Tag durch die Ablagen geht, VOR dem Bauen und **ohne dass Adam daran denken muss**. Deterministisch, kein Modell; **einer je Tag** ueber eine Datums-Marke, sonst waeche die Liste bei jedem Handstart. ⚠️ **A1 (20.08.): `failed/` ist kein Endlager mehr** — vorübergehende Fehler (Zeitüberschreitung, Netzfehler, nicht initialisierter HTTP-Client) wandern mit `versuche`/`nicht_vor` zurück in die outbox, wachsende Abstände, nach fünf Versuchen ins Endlager **mit Meldung**. **Gedrosseltes ebenso** — es lag vorher in `sent/` und sah aus wie zugestellt, der Ordner log. ⚠️ Die Liste der vorübergehenden Fehler ist **geschlossen**: Was nicht daraufsteht, gilt als dauerhaft — andersherum liefe ein dauerhafter Fehler fünfmal ins Leere ⚠️ **Knopf-Arten sind eine geschlossene Liste** (`KNOPF_ARTEN`, 20.08.) — der Ordner wird von mehreren Skripten beschrieben; ohne die Pruefung koennte jedes eine beliebige Schaltflaeche in Adams Chat setzen. Faellt eine Art durch, geht die Nachricht **ohne Knopf** hinaus, nicht gar nicht | `.venv/bin/python scripts/test_pruefumgebung.py` → vier Zeilen, darunter „jede Postfach-Nachricht nennt ihren Absender" |
| **Repo-Wächter: Fehlerumleitung ist kein Schreiben** (19.08., Claudias Befund): `_ohne_harmlose_umleitung`, `_repo_read_grund`, `_is_repo_write_cmd`, `_is_repo_read_cmd` in bot.py; Prüfer `scripts/test_update_textbefehl.py` | **Der Fix musste an ZWEI Stellen greifen — Geschwister-Regel in Reinform.** Die Lockerung im Lese-Zweig half zunächst gar nichts: Er befragt `_is_repo_write_cmd` als doppelten Boden, und dessen Muster sucht `>`, hielt `2>&1` also weiter für ein Schreibmuster. ⚠️ **Nur Fehlerkanäle** (`2>&1`, `2>/dev/null`, `2>/tmp/…`) — eine stdout-Umleitung bleibt draußen, `git log > /etc/passwd` ist genau der Fall, gegen den der Filter steht. ⚠️ **Vor der Suche entfernen, nicht danach ausnehmen**: Sonst verdeckte ein harmloses `2>&1` ein echtes Metazeichen im selben Befehl. ⚠️ **`_repo_read_grund` nennt das beanstandete ZEICHEN** (Claudias Zusatz) — ohne das rät der Empfänger, was er falsch gemacht hat; sie selbst hat daraufhin eine falsche Ursache diagnostiziert. ⚠️ **„Abgelehnt" heißt Freigabe-Dialog, nicht blockiert** — diese Unterscheidung fehlte im Befund | `.venv/bin/python scripts/test_update_textbefehl.py` → darunter „die Riegel halten trotzdem" |
| **Quittung des Log-Abgleichs** (19.08., Nachlese ②/③): `letzter-abgleich.txt` in `scripts/log_sync.sh` | **Die Abmachung versprach eine Meldung, das Skript lieferte keine** — dritter Fund der Klasse „eine Vorgabe, die nur auf dem Papier steht" an einem Tag. Claudia fragte deshalb, ob sie nachsehen solle, ob ihre Fracht angekommen sei; die Quittung beantwortet das von selbst. ⚠️ **Sie nennt auch, was NICHT mitkam, samt Grund je Datei.** Der Geheimnis-Namensfilter bleibt hart, wird aber sichtbar: Eine Ausarbeitung über „Token-Optimierung" darf ausgefiltert werden — nur nie lautlos. Sichtbar aussortiert ist sicher und ehrlich; lautlos aussortiert ist die nächste Stille, die wie Ruhe aussieht. ⚠️ Der Grund „unklar — bitte melden" ist ein Fehler auf unserer Seite und ausdrücklich als solcher benannt | `bash scripts/test_zielumgebung.sh` → „Abgleich quittiert Mitgenommenes und Ausgeschlossenes" |
| **Wiederkehrende Aufträge E3** (19.08., Adams Entscheid 3a): `WIEDERKEHR_ARTEN`, `wiederkehr_erlaubt`, `_faellig`, `WIEDERKEHR_FEHLGRENZE` in `scripts/hora.py`; Prüfer `scripts/test_hora.py` | **Ein Auftrag, der von selbst wiederkommt, ist kein Auftrag mehr, sondern ein Prozess** — deshalb die geschlossene Liste rein lesender Prüf-Arten; alles Verändernde bleibt einmalig, ohne Ausnahme. ⚠️ **Geprüft am ÜBERGANG, nicht am Eintrag**: Die Angabe im Auftrag ist ein Vorschlag, nie eine Wahrheit — dieselbe Bauart wie der Riegel im Auftragsbuch, wo eine handgelegte Grün-Behauptung durchrutschte, weil die Ampel aus der Datei gelesen wurde. Fällt eine Art später aus der Liste, hören auch liegende Aufträge auf zu wiederkehren. ⚠️ **Adams Festlegung (d) musste präzisiert werden**: Sein Wortlaut nennt die Fehlserie — die wird aber bei JEDEM Erfolg genullt, und neben einem grünen Auftrag erreichte der globale Zähler die Grenze nie. Deshalb ein **eigener Zähler je Auftrag** (`wiederkehr_fehler`), der nach drei eigenen Fehlläufen die Wiederkehr aussetzt und das meldet. Bei Erfolg wird er zurückgesetzt, sonst summierte er über Wochen. ⚠️ **Vor Fälligkeit übersprungen, OHNE Meldung** — ein Wiederkehrer, der zwölfmal am Tag sagt, dass er noch nicht dran ist, macht die Liste unlesbar. ⚠️ **Ein unverständlicher Zeitstempel gilt als fällig**: Stillstehen ist die schlechteste Antwort (Lehre Versions-Monitor) | `.venv/bin/python scripts/test_hora.py` → darunter „nur lesende Arten duerfen wiederkehren (a)" und „ein roter Wiederkehrer rennt nicht ewig (d)" |
| **Update-Auslösung per Text E4** (19.08., Adams Entscheid 4a): `cmd_update_ja` / `cmd_update_nacht` / `_e4_ausloesen` in bot.py, an denselben `upd:`-Pfaden wie die Knöpfe; Prüfer `scripts/test_update_textbefehl.py` | **Ein Befehl, kein Satz — weil dieser Pfad Pakete auf dem Server austauscht.** Ein Knopf lässt sich nicht aus Versehen tippen, ein Satz schon; und ein Modell, das „ja mach das Update" auslegen müsste, brächte Ermessen dorthin, wo keines hingehört. ⚠️ **Kein Fuzzy, kein Freitext-Handler** — Telegram reicht nur exakte Kommandos an einen `CommandHandler`; wer sich vertippt, löst nichts aus und sein Text geht als normale Nachricht an den Agenten. ⚠️ **Keine Parallel-Logik**: derselbe Updater, dieselbe Drift-Sperre (A3), dasselbe Wartungsfenster wie am Knopf — eine zweite Wahrheit über dieselbe Frage driftet (G1-Lehre). ⚠️ **Nur Adams `user_id`**, und eine fremde Kennung bekommt auch **keine Auskunft** — gegengeprüft durch Entfernen der Autorisierung. ⚠️ Freigegeben wird **genau die angezeigte Fassung**: Ist inzwischen eine neue erschienen, wäre Adams Ja ein Ja zu etwas anderem | `.venv/bin/python scripts/test_update_textbefehl.py` → sieben Zeilen, darunter „fremde user_id loest nichts aus und bekommt keine Auskunft" |
| **Kontingent-Abrufer** (20.08., A2): `scripts/kontingent.py` (ROLLE: `kontingent-abrufer`) — **NICHT IN BETRIEB**, von nichts aufgerufen; Befund `docs/befund-a2-kontingent.md` | **Gebaut, aber der Zugang fehlt.** Der undokumentierte Kontostand-Endpunkt lehnt das Abo-Setup-Token ab — **403 in drei Kopfzeilen-Varianten**, also erkannt und nicht berechtigt (nicht 404: der Endpunkt existiert; nicht 401: das Token wird erkannt). ⚠️ **Bleibt liegen, wird nicht eingebunden**, bis Adam über den zweiten Weg entscheidet (Sitzungs-Token per `claude login` auf dem VPS — **nicht empfohlen**: zweite Stelle mit Kontozugang). ⚠️ Das Token wird **nur aus der Umgebung** gelesen und **nie** in Kommandozeile, Protokoll oder Fehlermeldung gezeigt — deshalb faengt der Aufruf jede Ausnahme selbst ab (eine `HTTPError` traegt die Anfrage samt Authorization-Kopfzeile). 💰 Keine Kosten: derselbe Anbieter, derselbe Abo-Zugang, ein GET auf einen Zaehlerstand | `.venv/bin/python scripts/kontingent.py --probe` → erwartet heute `NICHT VERFÜGBAR: HTTP 403`; eine Auslastungszeile hiesse, der Zugang ist da — dann gehoert der Befund neu bewertet |
| **Erinnerungs-Laeufer** (20.08., Punkt 7.2): `scripts/erinnerungen.py` (ROLLE: `erinnerungs-laeufer`) — **RUHEND**, kein Zeitgeber eingespielt; nutzt `kalender.py` und `botenpost.legen`; Vorlage in `docs/befehlsbloecke-root.md`; Pruefer `scripts/test_erinnerungen_7_2.py` | **Termine kommen von selbst, statt dass Adam `/termine` tippt.** ⚠️ **Zwei Dinge fehlen, beide bei Adam:** der iCloud-Zugang (7.3) und der Erinnerungskanal (7.1) — **ohne Kanal hat er kein Ziel** und legt solange in den Bot-Chat, was er **ausdruecklich sagt** statt Vollstaendigkeit vorzutaeuschen. ⚠️ **Kein Modell im Pfad** (Pruefzeile sichert es): Ein Zeit-Trigger, der ein Modell startet, ist AGB-Grauzone; Adams Linien-Entscheid deckt nur den nachgeholten Lauf nach einem Limit. ⚠️ **Eine Meldung je Lauf, nicht eine je Termin** — die Postfach-Obergrenze von fuenf je Stunde wuerde sonst ausgerechnet die Erinnerung zurueckhalten, fuer die er gebaut wurde. ⚠️ **Gedaempft ueber den INHALT, nie ueber die Zeit** (28.07.-Lehre). ⚠️ Faellt eine Quelle aus, meldet die andere weiter **und der Ausfall wird benannt** | `.venv/bin/python scripts/test_erinnerungen_7_2.py` → 9 Zeilen, darunter die Zeile zum fehlenden Zugang |
| **Kontingent-Stand** (20.08., Punkt A2): `bot.py` `_limit_letzten_merken` / `_LIMIT_LETZTER` / `cmd_kontingent` / `_vor_wie_lange`; Marke `~/.claude/limit-stand.json` (env `LIMIT_STAND_FILE`); haengt an `_limit_warnung_melden` und am SDK-Ereignis `RateLimitEvent` | **Adam kann den Abo-Stand abrufen, ohne dafuer Kontingent zu verbrauchen.** Die Zahl steht in den Kopfzeilen jeder API-Antwort (`anthropic-ratelimit-unified-<fenster>-utilization`) und fliesst ohnehin durch den Nachrichtenstrom - **es gibt nichts abzufragen.** „Nicht baubar“ war vier geprueften Wegen lang die falsche Antwort (403-Endpunkt, CLI-Unterbefehl, eigener Zaehler, zweites Token). ⚠️ **Die Reihenfolge ist der ganze Punkt:** gemerkt wird VOR der Statusbewertung - darunter faellt jeder gruene Stand heraus. ⚠️ **Die Anzeige nennt immer das Alter** des Werts; eine Zahl ohne Alter waere die stille Falsch-Wahrheit. ⚠️ **`[GEAENDERT 20.08., A2.2]` Der Abruf loest bei LEEREM Merker EINEN Modelllauf aus** (kleinstes Modell, kuerzeste Frage) und sagt das im Text — bei bloss ALTEM Stand nicht, dort entscheidet Adams Schaltflaeche. Zulaessig, weil mensch-initiiert: ein Tipp, ein Lauf. **Genau zwei Aufrufstellen erlaubt** (Befehl + Schaltflaeche); eine dritte, besonders ein Zeitgeber, waere ein AGB-Bruch und wird von der Pruefzeile gefangen | `.venv/bin/python scripts/test_kontingent_a2.py` → 9 Zeilen, darunter „gemerkt wird VOR der Bewertung“ |
| **Kontingent-Sitzung** (20.08., A2.2): `kontingent_sitzung.py` (ROLLE: `kontingent-sitzung`) `auslesen()`; genutzt von `bot.py` `_kontingent_frisch_messen`; eigene Ablage `~/.claude/kontingent-sitzung` (env `KONTINGENT_HOME`); braucht die gebuendelte Oberflaeche des SDK (`claude_agent_sdk/_bundled/claude`) | **Die Prozentwerte des Abos, die es sonst nirgends gibt.** Gemessen: Endpunkt 403, SDK-Ereignis ohne `utilization`, Stapel-CLI ohne, Statusline nur interaktiv — die Zahl gibt allein die **Oberflaeche** heraus. Deshalb echte Sitzung am Pseudo-Terminal, `/usage` getippt, Bildschirm gelesen. ⚠️ **`/usage` ist LOKAL** (`Total cost: $0.0000`, `API: 0s`) — **kein Modell-Aufruf, kein Kontingentverbrauch, keine AGB-Frage**; es kostet nur etwa eine Minute. ⚠️ **Gelesen wird ein Bildschirm, keine Schnittstelle** — aendert sich das Layout, liefert das Muster nichts; der Fehlschlag ist bewusst sauber (kein geratener Ersatz). ⚠️ **Die eigene Ablage ist Pflicht**, sonst haengt die Sitzung im Einstiegsdialog und **verbraucht die Eingabe**, die `/usage` sein sollte (zweimal erlebt) ⚠️ **Die Ampelschwellen sind ADAMS** (gruen bis 50, gelb ab 51, orange ab 71, rot ab 86), nicht die des Anbieters — dessen Warnung kommt erst kurz vor Schluss. ⚠️ **Der Ruecksetzzeitpunkt wird EXAKT genannt** (Spanne + Uhrzeit + Wochentag) — hier gilt die Grob-Regel fuer Zeitangaben ausdruecklich NICHT, Adam will planen | `.venv/bin/python kontingent_sitzung.py <pfad-zur-oberflaeche>` → Anteile je Fenster; im Bot ueber `/kontingent` bzw. den Knopf |
| **Tagescheck-Ausgaenge** (21.08.): `scripts/daily_check.sh` `trocken()` + die DREI Ausgaenge (`merken` = Protokoll, curl-Versand, Sichtungs-Vermerk); Schalter `TROCKENLAUF`, Umleitung `AUFTRAGSBUCH_DIR` (mit Rueckfall in `BOTENV`); geprueft von `scripts/test_zielumgebung.sh` | **Ein Pruefer darf den Waechter nicht in Adams echte Ablagen schreiben lassen.** Der Zielumgebungs-Pruefer startet den Tagescheck vollstaendig, und der ruft den Regressionstest, der jenen Pruefer enthaelt — eine Schachtelung auf sich selbst. Am 21.08. gemessen: Der innere Lauf brach an **Dateirechten** ab, nicht an einem Riegel; als root gestartet haette er geschrieben. ⚠️ **Der Schalter sitzt an den AUSGAENGEN, nicht an den Aufrufstellen** — eine Liste von Stellen waechst, und jede vergessene ist ein Leck. ⚠️ **Der curl-Versand ist BEWUSSTE Redundanz neben dem Boten-Postfach**, kein uebersehener Doppelweg: Der Tagescheck muss einen **toten Bot** melden koennen, und das Postfach laeuft durch den Bot-Prozess — es waere im wichtigsten Stoerfall stumm. **Nicht als „zwei Wege fuer dasselbe“ aufraeumen.** ⚠️ Der Normalfall wird MITgeprueft (ohne Schalter muss der Vermerk entstehen), sonst belohnt der Pruefer Untaetigkeit | `bash scripts/test_zielumgebung.sh` → 20 Zeilen, darunter „Trockenlauf legt nichts ab“ und „ohne Schalter entsteht der Vermerk“ |
| **Mail-Konto-Helfer** (21.08., Punkt 9.5): `scripts/mail_konto_anlegen.sh` (ROLLE: `mail-konto-helfer`); schreibt `MAIL_<NAME>_{ADRESSE,BENUTZER,KENNWORT,IMAP,SMTP,ALIASSE}` nach `/etc/claude-telegram-bot.env`; gelesen von `email_kanal.py` `_konten()` | **Adam legt Postfaecher an, ohne dass ein Kennwort irgendwo sichtbar wird.** ⚠️ **Ein Befehlsblock zum Kopieren waere hier FALSCH gewesen:** Beim Einfuegen mehrerer Zeilen liest `read` die naechste eingefuegte Zeile als Antwort — das Kennwort wuerde nie abgefragt und in der Datei stuende Unsinn. Deshalb ein Skript mit `< /dev/tty`. ⚠️ **Der Helfer prueft die Zugangsdaten NICHT** — ein Verbindungstest braeuchte das Kennwort in einem weiteren Prozess; falsch heisst hier: der Bot sagt es beim ersten Abruf. ⚠️ **Doppelte Kurznamen werden abgewiesen**, weil doppelte Zeilen in der Umgebungsdatei sich still ueberschreiben. ⚠️ Der Dienst laedt die Umgebung **erst beim Neustart** | `bash -n scripts/mail_konto_anlegen.sh`; nach dem Anlegen im Bot `/mail` — die Konten muessen dort auftauchen |
| **Eingangs-Absicherung** (22.08., Punkte 1-10): `bot.py` `on_my_chat_member` (Absender) / `werkzeugfreie_optionen` + `_WERKZEUGE_VERBOTEN` / `_extract_hosts(fuer_vertrauen=)` + `_DATEIENDUNGEN_KEINE_DOMAIN` + `_such_ids` / `_is_sensitive_ref(schreibend=)` + `_GEHEIMNIS_MARKER` / `_DAUERWIRKUNG_MARKER` + `_UMGEBUNGS_BEFEHL_RE` + `_glob_zielt_auf_geheimnis` / `freigaben_bereinigen` + `_NO_ALWAYS_TOOLS` / Recall-Kopf / `Defaults(link_preview_options)`; `presend.py` `_scharfe_befehle` + `_SCHARFE_MUSTER` | **Kein Fremdinhalt erreicht eine Handlung.** Adams Grundsatz: Einen Auftrag geben heisst, das LESEN zu beauftragen. ⚠️ **Die Schranken fragen NIE „ist das ein Befehl?“** — das waere eine Inhaltsfrage, und Inhalt laesst sich tarnen (weisse Schrift, Zero-Width-Zeichen, Text in Bildern). Sie fragen nur, ob Fremdinhalt eine Handlung erreicht. ⚠️ **(6) und (10) ersetzen einander NICHT:** Dauerfreigabe weg schliesst den bequemen Weg, die Platzhalter-Luecke den getarnten — ohne (6) kaeme zwar eine Rueckfrage, aber mit einem harmlos aussehenden Befehl. ⚠️ **`allowed_tools=[]` heisst NICHT „keine Werkzeuge“** (SDK: `bypassPermissions` genehmigt alles vor dem Rueckruf) — nur `dontAsk` sperrt. ⚠️ **Nie eine Schranke bauen, die alles abweist:** jede Pruefzeile hat eine Gegenrichtung, sonst belohnt sie Untaetigkeit | `.venv/bin/python scripts/test_eingangsschranken.py` → alle Zeilen gruen (**keine Zahl hier**: sie war am 23.08. auf 17 stehengeblieben, waehrend es 40 waren — eine Zahl, die von Hand nachgepflegt werden muss, wird irgendwann nicht nachgepflegt; Engywucks Befund F-7) |
| **Redeseiten-Messung** (23.08.): `scripts/mess_redeseite.py` (ROLLE: `mess-redeseite`); nutzt `bot.mail_zusammenfassen` und den Korpus; Ergebnis `docs/messung-redeseite-23-08.md` | **Kommt der Inhalt als Zitat an, oder uebernimmt der Bot die Aufforderung?** ⚠️ **KEIN Pruefer, sondern ein Messwerkzeug** — es startet echte Modell-Laeufe. Ein Pruefer, der bei jedem Regressionslauf Kontingent verbraucht, wird abgeschaltet und prueft dann gar nichts mehr. Deshalb NICHT im Laeufer, sondern von Hand: vor dem ersten echten Postfach, und wenn sich System-Prompt oder Berichtskopf aendern. ⚠️ **Vier Faelle als Vorgabe, nicht 22** — drei Tarnungen plus EIN Kontrollfall; ohne den misst man nur, ob der Bot misstrauisch ist, nicht ob er brauchbar bleibt. ⚠️ **Die Wortlisten sind eine HEURISTIK** und werden als solche berichtet — sie richten den Blick, sie ersetzen kein Lesen. „Unauffaellig" heisst: kein Marker gefunden, nicht: von einem Menschen ganz gelesen. ⚠️ **Die Redeseite ist grundsaetzlich nicht schliessbar** — was geschlossen ist, ist die Handlungsseite (`test_mailkorpus.py`, ohne Modell) | `.venv/bin/python scripts/mess_redeseite.py` → nur dort, wo das Abo-Token liegt (VPS) |
| **Mail-Text auf Abruf** (23.08., Stufe B1/B2/B3): `email_kanal.nachricht_text()` (readonly + BODY.PEEK, Kennung geprueft); `bot.mail_zusammenfassen()` ueber `werkzeugfreie_optionen`; Knoepfe je Nachricht in `on_mail_knopf` | **Ein Mailtext wird gelesen, nie befolgt — und der Lauf hat kein Werkzeug.** ⚠️ **KEIN Ausweichzweig in die Hauptsitzung** — das war Rang-1-Befund C, und er darf hier nicht neu entstehen; scheitert der Lauf, wird ehrlich gescheitert. ⚠️ **B2 ist BAUARTBEDINGT, nicht geprueft:** Der Lauf geht an der Warteschlange VORBEI (eigene Optionen, eigener Client, kein QueuedJob) — es gibt gar keinen Weg zu `task_origins`. Das ist staerker als ein Filter, der abweist. ⚠️ **Zweistufig:** der Text kommt erst auf Knopfdruck, damit Fremdtext aus dem Zusammenhang bleibt, solange ihn niemand braucht. ⚠️ **Anhaenge werden NICHT beruehrt**, auch nicht zum Anzeigen — eigene Risikoklasse, eigener Auftrag. ⚠️ **Der System-Prompt ist eine BITTE, nicht die Zusage** — die Zusage ist die Werkzeugfreiheit: Was der Lauf sagt, erreicht nur Adam; was er tun koennte, gibt es nicht | `.venv/bin/python scripts/test_mailkorpus.py` → Zeilen (B1), (B2), „der Textabruf bleibt nur lesend", „unbrauchbare Kennung wird abgewiesen" |
| **Mailtext-Entschaerfung + Angriffskorpus** (23.08., Stufe B4): `mailtext.py` (ROLLE: `mailtext`) `lesbar()` / `bericht()` / `_Leser` / `_UNSICHTBAR_STIL` / `_UNSICHTBARE_ZEICHEN` / `MAX_ZEICHEN`; Korpus `tests/mailkorpus/*.eml` (22 Faelle, davon 4 Kontrollen); Pruefer `scripts/test_mailkorpus.py` | **Was fuer das Auge nicht dasteht, muss trotzdem BENANNT werden.** Adams Grundsatz vom 21.08.: Anweisungen lassen sich unsichtbar legen — weisse Schrift, `font-size:0`, `display:none`, HTML-Kommentare, `alt`/`title`, Preheader. ⚠️ **Gewaehlt ist MITLESEN UND MARKIEREN, nicht Entfernen** — und das ist die Kernentscheidung: Eine Mail, deren versteckter Teil spurlos verschwindet, sieht harmlos aus, und Adam erfuehrt nie, dass jemand etwas zu verbergen versuchte. **Dass jemand versteckt hat, IST die Information.** ⚠️ **Gemessen wird das Merkmal, nicht das Aussehen** — „fast weiss" liesse sich endlos variieren, `display:none` nicht. ⚠️ **KEIN NETZ:** externe Bilder, Stylesheets und Schriften werden gelesen, nie abgerufen — ein Zaehlpixel verraet bereits, dass gelesen wurde. ⚠️ **Der Korpus wird DURCHLAUFEN, nicht aufgezaehlt**; wer eine Datei dazulegt, hat sie im Pruefer. ⚠️ **Die vier Kontrollfaelle sind Pflicht** — ein Pruefer ohne Gegenrichtung belohnt Untaetigkeit: Wer alles abweist, ist gruen und nutzlos. ⚠️ **Die REDESEITE steht noch aus** (kommt der Inhalt als Zitat an?) — sie braucht einen Modell-Lauf; die Handlungsseite ist geschlossen, die Redeseite nur messbar, nie schliessbar. Die tragende Zusage bleibt „kein Schaden ohne seinen Daumen", nicht „kein Text taeuscht ihn je" | `.venv/bin/python scripts/test_mailkorpus.py` → 9 Zeilen ueber 22 Faelle |
| **Mail-Abruf Stufe A** (23.08., Punkt 9.5): `email_kanal.py` `posteingang()` / `_kopf_zerlegen()` (ueber `email.parser`) / `posteingang_lesbar()` / `_neutral()` + `_FORMATZEICHEN` / `_STEUERZEICHEN` / `MAX_ABRUF` + `ABRUF_FRIST_S`; verdrahtet in `bot.py` `cmd_mail` (`/mail <konto>`) und `on_mail_knopf`; Messwerkzeug `scripts/mess_kopfzeilen_a1.py`; Befund `docs/befund-a1-kopfzeilen.md` | **Adam sieht seinen Posteingang — und Fremdtext kann dabei bauartbedingt nichts anweisen.** ⚠️ **KEIN MODELL im Pfad, ausdruecklich:** weder im Befehl noch in der Aufbereitung laeuft ein Agent. Fremdtext erreicht keine Handlung, weil es nichts gibt, das er anweisen koennte — das Ruecklaufventil in Reinform, kein Filter, der Inhalt beurteilt. ⚠️ **KEIN ZEITGEBER, ausdruecklich** — ein zeitgesteuerter Abruf waere ein Modell-Lauf ohne Adams Zutun (AGB-Leitplanke). ⚠️ **Die Zerlegung gehoert `email.parser`, nicht uns:** Die alte Handschleife las eine FORTSETZUNGSZEILE als eigenes Feld, weil `name.strip()` genau den Marker entfernt, der sie als Fortsetzung ausweist — Absender faelschbar. Gegenprobe in beide Richtungen gefahren. ⚠️ **`_STEUERZEICHEN` muss die Nicht-ASCII-Trenner fassen** (U+0085/2028/2029) und die Zero-Width-Zeichen: gemessen kamen sie durch. Ersetzt, nicht entfernt — ein Zeichen, das spurlos verschwindet, verbirgt, dass es da war. ⚠️ **Zwei Riegel gegen Formatierung** (`_neutral` + Senden ohne `parse_mode`), weil eine Sendestelle irgendwann jemand aendert. ⚠️ **Der Deckel ist HART**, nicht nur Vorgabe — Fremdtext ist das, wovon so wenig wie moeglich hereinkommen soll. ⚠️ **Nie eine leere Liste bei Stoerung:** leer heisst „keine Post", und das waere eine beruhigende Falschauskunft. ⚠️ **Stufe B (Text auf Abruf) ist NICHT gebaut** — eigener Auftrag, mit Angriffs-Korpus | `.venv/bin/python scripts/test_email_9_5.py` → Zeilen „die Uebersicht ist verdrahtet (Stufe A)", „(A1)", „(A2)", „(A3)"; dazu `.venv/bin/python scripts/mess_kopfzeilen_a1.py` → 24 Varianten |
| **Differenzmesser** (23.08., Engywucks Bauauftrag): `scripts/differenz.py` (ROLLE: `differenzmesser`) — drei Arten (`module_differenz`, `ablagen_differenz`, `festpfade_differenz`), Sammler ueber den EIGENEN Syntaxbaum, Gegenprobe als LADEBEDINGUNG; Pruefstand `scripts/test_hermetik.py`; verriegelt die Wegwerf-Umgebung in `scripts/regressionstest.sh` | **Mengen statt Aufzaehlungen — die Ablage ist eine zweite, handgepflegte Kopie von Tatsachen, die im Code schon stehen, und jede handgepflegte Kopie driftet.** Gemessen: 84 Posten, 45 davon Aufzaehlungen, wo eine Menge hingehoert. ⚠️ **Das Lehrstueck steht im Selbstcheck selbst:** `_c_register_vollstaendig` hat zwei Haelften — der Skript-Teil bildet eine Menge (gut), der Modul-Teil war eine fest verdrahtete Siebenerliste und erfasste sieben von achtzehn. Dass er einmal `ampel.py` „fand", lag daran, dass es DARIN STAND; genau dieser Satz wurde jahrelang als Beleg fuer die Mengen-Regel weitergereicht. ⚠️ **Die Ist-Menge NIE ueber die Endung:** Meine eigene Vorwegnahme vom selben Tag suchte `_DIR`/`_FILE` und verfehlte damit die AMPEL (Klienten-Namen, laut CLAUDE.md das Heikelste im Projekt) — ein Pruflauf haette ihre Regeldatei ueberschreiben koennen, lautlos. ⚠️ **Die Gegenprobe ist LADEBEDINGUNG, kein Lauf:** Wer eine Art ohne `<name>_gegenprobe` hinzufuegt, bekommt einen Fehler statt einer stillen Aufnahme — eine Art, die nie etwas meldet, sieht sonst aus wie eine, die passt. ⚠️ **Kein neuer Waechter, kein Zeitgeber, keine Ablage, kein Modellaufruf** — ein bestehender Waechter war erweiterbar, damit ist der Kurs-Regel-Nachweis gefuehrt. ⚠️ **Noch NICHT im Selbstcheck eingehaengt** (Regel ①a: gebaut-und-ruhend darf warten, gebaut-und-wachend nicht) — das geschieht nach Engywucks Gegenpruefung | `.venv/bin/python scripts/test_hermetik.py` → Ladebedingung, alle Gegenproben, dann der Lauf |
| **Adam-Anteil** (23.08., Befund A/J): `bot.py` `_adam_anteil()`; verdrahtet im Texthandler, im Sprachpfad, im Dokumentpfad und in `_link_ablegen`; persistiert in `pending.record` und wiederhergestellt in der Wiederaufnahme; `linkinbox.Eintrag.eigenes_wort` | **Nur was Adam SELBST geschrieben hat, darf die Vertrauensliste speisen.** Ein Hostname aus seinem Satz wird ohne Rueckfrage abgerufen — deshalb ist die Frage, wessen Wort das ist, keine Formalie. ⚠️ **Weiterleiten ist Adams alltaeglichste Geste**, und `bot.py` enthielt null Vorkommen von `forward_origin`: Eine weitergeleitete Nachricht mit „Details unter shop-boese.tld" trug den Host ein. **Der Kommentar an der Stelle war die falsche Aussage, nicht der Code.** ⚠️ **Alle fuenf Weiterleitungsfelder plus `is_automatic_forward`** — ein einzelnes zu pruefen laesst die anderen offen. ⚠️ **Fail-closed:** Im Zweifel `None`; der Preis ist eine Rueckfrage zu viel. ⚠️ **Muss PERSISTIERT werden**, sonst verhaelt sich dieselbe Nachricht vor und nach einem Neustart verschieden — ein Unterschied, den niemand sieht und niemand erklaeren kann. ⚠️ **`eigenes_wort` hat Vorgabe FALSE**, damit Altbestand in der Link-Ablage kein Vertrauen erbt | `.venv/bin/python scripts/test_eingangsschranken.py` → Zeilen „eine Weiterleitung ist nicht Adams Wort" und „der Adam-Anteil ueberlebt einen Neustart" |
| **Pfad-Aufloesung statt Zeichenkettenvergleich** (23.08., Befund D/E): `bot.py` `_alle_pfade_im_repo()` (nutzt `shlex.split` + `Path.resolve()` gegen `_REPO_DIR`), gerufen von `_is_repo_read_cmd` | **Ein `..` hebt die Zusage auf, ohne die Zeichenkette anzutasten.** Gemessen liefen `cat <repo>/../../../etc/passwd` und `cat $X/proc/self/environ <repo>/README.md` ohne Dialog durch — letzteres gibt den Bot-Token und das Abo-Token aus. ⚠️ **H6 hatte die zwei Beispiele aus seinem eigenen Docstring geschlossen und die Klasse darueber verfehlt** — der Fehlertyp, den dieses Projekt mehrfach aktenkundig hat. ⚠️ **Drei fail-closed-Entscheidungen mit Grund:** unbalancierte Anfuehrungszeichen (wenn die Zerlegung nicht eindeutig ist, ist es die Bedeutung auch nicht), jedes `$` (der Wert ist hier nicht bekannt — genau darauf beruhte E), Argumente ohne Schraegstrich uebersprungen (ein Ausbruch braucht einen Pfad). ⚠️ **NEBENWIRKUNG, die drei Pruefer traf:** Sie trugen den VPS-Pfad fest ein und waren solange pfadunabhaengig gruen, wie nur Zeichenketten verglichen wurden — `scripts/test_hermetik.py` faengt das jetzt | `.venv/bin/python scripts/test_eingangsschranken.py` → Zeilen „`..` hebelt die Pfadpruefung nicht aus" und „eine Variable macht keinen Pfad unsichtbar" |
| **Hermetik der Pruefläufe** (23.08., Befund L): `scripts/test_hermetik.py` (ROLLE: `test-hermetik`) — zwei Pruefungen: Ablagen ohne Riegel im Regressionslaeufer, und fest verdrahtete Betriebspfade in Pruefern; dazu `bot.py` `_PREFS_FILE` aus `USER_PREFS_FILE` und die zehn `export`-Zeilen in `scripts/regressionstest.sh` | **Kein Pruflauf darf Betriebszustand anfassen.** Zwoelf Testdateien setzten `USER_PREFS_FILE` und hielten sich fuer isoliert — `bot.py` hat die Variable **nie gelesen**. Auf dem VPS standen danach alle drei Kanal-Kennungen auf der Test-Attrappe `-1001234567890`; der Bot haette alle Ausgaben in einen Kanal gelenkt, den es nicht gibt, **ohne Fehlermeldung**. ⚠️ **Eine gesetzte Variable, die niemand liest, sieht genauso aus wie eine, die wirkt** — daran blieb es unbemerkt. ⚠️ **Der Laeufer trug den Merksatz „wer eine neue Zustandsablage einfuehrt, traegt sie im selben Zug ein" seit dem 20.08. — und war trotzdem an fuenf Stellen unvollstaendig.** Eine Regel ohne Pruefer ist eine Bitte. ⚠️ **Der Pruefer misst eine ABWESENHEIT** (der erlaubte Fall); die Wirkung misst `test_eingangsschranken.py` am Zustand der geladenen Module. ⚠️ **Die Pfad-Haelfte musste dreimal enger gefasst werden** — ihr erster Entwurf schlug bei sieben Stellen an, davon sechs berechtigt, eine davon seine eigene Erklaerung | `.venv/bin/python scripts/test_hermetik.py` → beide Haelften; im Regressionslauf als „Hermetik der Pruefläufe (L)" |
| **Nie-dauerhaft-Menge** (`_NO_ALWAYS_TOOLS`, bot.py): `Bash` `Write` `Edit` `MultiEdit` `NotebookEdit` `WebFetch` + Kosten-Werkzeuge; verdrahtet im Always-Zweig, im Knopf-Angebot, beim Speichern und in `freigaben_bereinigen` | **Ein einziger Klick auf „immer erlauben“ darf nicht unsichtbar fortgelten.** ⚠️ **DAS AUFNAHMEKRITERIUM ist die EIGENSCHAFT, nicht der Name:** Ein Werkzeug steht hier, weil seine **Wirkung ueber die Sitzung hinausreicht** — nicht, weil es „Bash“ heisst. Wer die Menge aendert, prueft diese Eigenschaft. Ohne diesen Satz liest jemand in drei Wochen sechs Namen und sieht keinen Grund, warum `MultiEdit` drinsteht und ein neues Werkzeug nicht. ⚠️ Schreibende Werkzeuge sind hier **so maechtig wie Bash**: der Gedaechtnis-Ordner geht in den System-Prompt jeder kuenftigen Sitzung, eine `hooks`-Sektion fuehrt Befehle aus. ⚠️ Die Bereinigung wirkt **rueckwirkend** auf gespeicherte Freigaben | `.venv/bin/python scripts/test_eingangsschranken.py` → Zeilen „auch Write/Edit nicht dauerfreigebbar“ und „gespeicherte Bash-Freigabe wird geraeumt“ |
| **Kanal-Verweise** (6.2/6.4): `bot.py` `_channel_title_link_html` (Textlinks, EINZIGE Quelle), `_channel_post_markup` (Inline-Knoepfe), `_channel_url` / `_channel_post_url` (Bausteine); fuenf Aufrufstellen | **Adam springt aus einem Hinweis direkt in den Kanal.** ⚠️ **Textlink und Knopf sind NICHT dasselbe:** tg://-**Textlinks** loesen einen Bestaetigungsdialog aus, **Knoepfe** nicht — deshalb darf der Knopf den Deep-Link gefahrlos nutzen, waehrend beim Textlink offen ist, ob der gesparte Browser-Umweg den zusaetzlichen Tipp wert ist (**Adams iPhone-Test**, 6.2). ⚠️ **Neue Kanal-Verweise NIE von Hand als `<a href>` bauen** — sonst entscheidet die Linkart wieder an mehreren Stellen; eine Pruefzeile faengt es ab | `.venv/bin/python scripts/test_kanal_links_6_2.py` → 5 Zeilen, darunter „nur eine Stelle entscheidet“ |
| **Pin-Bezug** (20.08., Punkt 5.13): `bot.py` `_pin_bezug` + `on_pinned_message`; nutzt `_channel_post_url`; schreibt `telegram-pinned.md` und `~/notes/telegram-notes.md` | **Ein angepinnter Merker behaelt seinen Rueckweg zum Original.** ⚠️ **Gruppe und Privatchat sind NICHT gleich:** in Gruppen/Kanaelen (`-100`-Kennung) gibt es eine adressierbare Nachricht und damit einen Deep-Link; **im Privatchat gibt es keine** — dort steht die Nachrichtennummer allein, und das ist die vollstaendige Wahrheit, kein Notbehelf. ⚠️ Beide Zweige (Memory **und** persoenliche Notiz) tragen denselben Bezug (Geschwister-Regel) | `.venv/bin/python scripts/test_pin_bezug_5_13.py` → 5 Zeilen, darunter „Privatchat bekommt KEINEN Link“ |
| **Log-Wachposten** (19.08., Engywucks Bauauftrag): `scripts/wachposten.py` (ROLLE: `log-wachposten`) + `wachmuster.py` (ROLLE: `wachmuster`) + Zeitgeber `wachposten.timer` (5 min, `User=claudebot`) + Doku `docs/wachposten.md`; nutzt `botenpost.legen` und `ampel.classify`; Prüfer `scripts/test_wachposten.py` | **Die Instanz, die unaufgefordert warnt — ohne Modell im Pfad.** Vollautonomes Modell-Wachen ist AGB-Grauzone, also deterministisch: Muster statt Urteil, Kosten null. Er zeigt, er entscheidet nicht. ⚠️ **Die Meldung fragt nicht, sie bietet an** (Adam 20.08.): Bis dahin endete sie mit „Engywuck wecken?“ — eine Frage ohne Weg, Adams Daumen darauf blieb wirkungslos. Jetzt traegt sie eine **Schaltflaeche**, die den Befund deterministisch ins Auftragsbuch legt (gelb, also zur Vorlage — zugestimmt wird dem Hinterlegen, nicht dem Bauen). ⚠️ **Die Schwelle hängt an der QUELLE, nicht am Muster** (gemessen an echten Daten): In `bot-errors.log` ist jede neue Zeile der Befund — dort nach Fehlermerkmalen zu suchen hieße zu prüfen, ob ein Fehler einer ist. Die reale Zeile `TimedOut: Timed out` traf im ersten Entwurf kein einziges Muster. ⚠️ **Die Gesprächsprotokolle stehen auf AUS** (`WACHPOSTEN_GESPRAECHE`) — sie sind das, was Adam privat schreibt; der Schalter ist seine Entscheidung. ⚠️ **Bei Ampel-ROT kein Wortlaut**, nur Quelle, Zeit und Kategorien-Label, und die Zurückhaltung wird BENANNT. `classify()` liefert `matches` mit den Treffern — die dürfen die Meldung nie berühren, sonst zitiert sie genau das Wort, das sie zurückhält. **Ein Einstufungs-Ausfall zählt als Rot.** ⚠️ **Gedämpft wird über die KENNUNG, nie über den Text** — ein Zeitstempel im Befundtext hebelte den Dämpfer am 28.07. aus und verdoppelte den Lärm. ⚠️ **Unlesbarer Merkzettel wird GEMELDET**, nicht verschwiegen (Lehre Versions-Monitor); eine Ausnahme ist ein Befund, kein Abbruch | ⚠️ **A4 (20.08.): zwei Fassungen** — Adam bekommt einen deutschen Satz (Anzahl, Quelle in Alltagssprache, Ampel-Wort), die Einzelheiten gehen nach `logs/wachposten-archiv.log`. **Der Kurier muss diese Datei mitnehmen** (`log_sync.sh`), sonst erreicht das Archiv niemanden und A4 ist halb gebaut. Ampel-Wort und Dämpfer-Zählzeile stehen in BEIDEN Fassungen | `.venv/bin/python scripts/test_wachposten.py` → 23 Zeilen, darunter „bei Ampel-ROT kein Wortlaut, nur das Label" |
| **Zeitgeber-Wache, gehärtet** (19.08.): der Suchblock in `scripts/daily_check.sh` | **Drei Befunde an einem Tag, alle im Echtbetrieb belegt.** ⚠️ **Ein Timer, dessen Dienst LÄUFT, ist nicht still** — systemd führt ihn im SubState `running` ohne `NextElapse`; der Tagescheck prüft sich während seines eigenen Laufs und klagte sich selbst an. ⚠️ **Auch die Zeitgeber von der PLATTE** (`/etc/systemd/system/*.timer`): `list-timers` zeigt nur Geladenes — ein gelöschter Timer galt als in Ordnung. Die erweiterte Suche fand sofort einen achten (`kostenkontrolle-reminder`, Altlast vom 14.07. unter `/usr/local/bin`, den der alte Ein-Eintrag-Pfadfilter nie sah). ⚠️ **Bewusst Abgeschaltetes ist kein Befund**: `disabled`/`masked` gilt als gewollt — verdächtig ist nur `enabled` und trotzdem inaktiv. Ohne diesen Ausweg hätte die eigene Erweiterung ab sofort einen täglichen Fehlalarm erzeugt. ⚠️ **Monotone Zeitgeber** tragen ihren nächsten Lauf in `NextElapseUSecMonotonic` | `bash scripts/test_zielumgebung.sh` + `.venv/bin/python scripts/test_blinde_flecken_b6.py` → „Zeitgeber-Wache klagt keinen laufenden an" |
| **Gruendlich-Umschalter B3** (28.07.): `_BTN_THOROUGH` / `_BTN_THOROUGH_ACTIVE`, `_thorough_on` / `_set_thorough` (ueber `_USER_PREFS`), Tiefe in `ensure_session`, `_main_keyboard(..., user_id=)`; Pruefer `scripts/test_gruendlich_b3.py` | **Ein Zustand mit Haken statt einer Einmal-Aktion.** Adams Anlass war sichtbar: Der Knopf liess sich einschalten, aber nicht mehr aus. Die gefaehrlichere Haelfte war unsichtbar — das `close_session` je Anfrage haette im Dauerbetrieb **den Gespraechsfaden nach jeder Nachricht zerschnitten**, und das meldet niemand als Fehler: Es sieht aus wie Vergesslichkeit. ⚠️ **Die Tiefe wird in `ensure_session` erzwungen, nicht im Auftragslauf** — sonst verloere ein Modellwechsel sie still. ⚠️ **Beide Beschriftungen gehoeren in `_ALL_KEYBOARD_BTNS`**, sonst geht ein Druck auf den Haken-Knopf als FRAGE an den Agenten (am 23.07. mit dem Transkriptions-Knopf live passiert). ⚠️ **Bei aktivem Gruendlich traegt auch „Max“ den Haken** — ein Haken, der luegt, ist schlimmer als keiner. ⚠️ Der Zustand liegt in den Vorlieben, nicht im Arbeitsspeicher: sonst waere er nach jedem Neustart weg, waehrend Adam ihn fuer gesetzt haelt | `.venv/bin/python scripts/test_gruendlich_b3.py` → sieben Zeilen, darunter „KEIN close_session mehr im Gruendlich-Zweig“ |
| **Zustell-Marke** (26.07.): `zustellmarke.py` (ROLLE: `zustell-marke`) + `zustellung_pruefen`/`zustell_worker` in bot.py + Lesen in `stundenblume.zustellung_pruefen` | **Der Wächter, der prüft, ob TELEGRAM UNS erreicht** — alle anderen prüfen, ob *wir* leben. Fällt die Zustellung aus (Zertifikat, Adresswechsel, Firewall, verstellter Webhook), schweigt der Bot, während **jede Anzeige auf Grün steht**. ⚠️ **Der Schlüssel steht im Aufruf-Pfad** (Telegram nimmt ihn als Teil der Adresse entgegen) — deshalb wird **nie eine Ausnahme im Wortlaut** weitergereicht, nur ihr Typ, und aus jedem Text fliegt die **ganze Adresse**, nicht nur der erkannte Schlüssel: Ein unbekanntes Schlüsselformat würde die Maske sonst verfehlen. Derselbe Fund wie bei 5.34, nur diesmal bei uns selbst. ⚠️ **Im Bot, nicht in den Blumen:** Der Bot hat den Schlüssel ohnehin; die Blumen hätten ihn sonst auch — ein zweiter Ort für ein Geheimnis. ⚠️ Alarm nur bei **frischem** Fehler (unter drei Stunden) und Rückstau **ab zwanzig** — ein alter Fehler und ein einzelnes wartendes Update sind keiner, sonst wäre der Wächter binnen zwei Tagen abgeschaltet | `.venv/bin/python scripts/test_zustellwaechter.py` → acht Zeilen, darunter „der Schlüssel taucht NIRGENDS auf (vier Wege)" |
| **Anmelde-Marke** (26.07., G1): `authmarke.py` (ROLLE: `anmelde-marke`) + `is_auth_error` in bot.py + Setzen im Auth-Zweig + Löschen bei `ResultMessage` + Lesen in `stundenblume.anmeldung_pruefen` | **EINE Wortliste für zwei Leser.** ⚠️ Wer hier wieder eine eigene Liste in `bot.py` oder `stundenblume.py` anlegt, erzeugt genau den Drift, der den Wächter halb blind gemacht hatte: Von sieben Marken war eine in beiden, und `oauth token expired` traf `oauth token **has** expired` nicht. ⚠️ Die **Marke** ist der vorrangige Weg (eigenes Format, keine Journal-Rechte, unabhängig vom Wortlaut des Anbieters); die Journal-Suche ist nur das zweite Netz für den Fall, dass der Bot zum Schreiben nicht mehr kam. ⚠️ **Die Entwarnung gehört an dieselbe Stelle wie der Alarm** — wer das Löschen aus dem Erfolgspfad entfernt, erzeugt einen Wächter, der nie wieder schweigt. ⚠️ Kein Geheimniswert in der Marke: Schlüssel-ähnliche Zeichenketten werden vor dem Ablegen ersetzt | `.venv/bin/python scripts/test_stundenblumen.py` → Zeilen „EINE Wortliste (G1)", „Marke schlägt Journal (G1)", „kein Geheimniswert (G1)" |
| **E-Mail-Kanal 9.5** (26.07.): `email_kanal.py` (ROLLE: `email-kanal`) + `/mail` + `MAIL_<KONTO>_*` in der geschützten Umgebung; **setzt 9.4 voraus** | Senden ist die **einzige unwiderrufliche** Fähigkeit im Aufbau. ⚠️ `senden()` verlangt eine Freigabe-Kennung mit Urteil „freigegeben" — wer diesen Riegel entfernt, macht aus einem Boten einen Absender. ⚠️ **Ampel gelb und Rückweg leer** sind Absicht, nicht Nachlässigkeit: Damit fällt eine Mail nie in die Sammelfreigabe (Leitplanke 3). ⚠️ Kopffelder mit Steuerzeichen werden **abgewiesen, nicht gesäubert** — ein Umbruch im Betreff kann ein stilles Bcc erzeugen, und so etwas ist nie ein Versehen. ⚠️ Anhänge aus Geheimnis-Pfaden verlassen das Haus nicht, auch nicht auf Wunsch. ⚠️ Das Kennwort steht **nicht** im Konto-Datensatz (Datensätze wandern in Protokolle). ⚠️ Posteingang `readonly=True` + `BODY.PEEK`, nur Kopfzeilen: eingehende Mail ist **Fremdtext, kein Auftrag** | `.venv/bin/python scripts/test_email_9_5.py` → 13 Prüfungen, fast alle auf dem, was NICHT geschieht |
| **Link-Nachbedingung** (26.07., S1/G6): `QueuedJob.links_abhaken` + `_links_nachtragen` im Worker + `linkinbox.notieren` | Abgehakt wird **nur bei belegtem Erfolg** (`outcome == "beantwortet"`). ⚠️ **Die Nachbedingung MUSS am Auftrag hängen, nicht am Knopf-Handler:** `process_user_text` reiht nur ein und kehrt sofort zurück — ein Fehlerfang dort sichert nur das Einreihen und **sieht aus wie ein Riegel, ohne einer zu sein**. ⚠️ Ein gescheiterter Lauf legt den Eintrag zurück **mit Grund** (`notieren`, hakt nicht ab) und meldet es; ein Eintrag, der stillschweigend verschwindet, ist schlimmer als einer, der noch dasteht | `.venv/bin/python scripts/test_linkinbox_5_14.py` → „Erfolg hakt ab, Misserfolg nicht (S1)", „Reel ist Video (S2)", „Volltext-Auftrag nennt die Reihenfolge (S2)" |
| **Stundenblumen — Belegkette** (25.07.): `scripts/stundenblume.py` + Kette `~/.claude/stundenblumen/kette.jsonl` + Ruhe-Marke + Prüfzeile im 4-Uhr-Check | Belegt die Zeit **zwischen** den Zeitpunkt-Prüfungen: Das **Ausbleiben** der Übergabe ist der Alarm. ⚠️ **Die Abdrücke müssen NACHGERECHNET werden, nicht nur verglichen** — der erste Testlauf deckte auf, dass eine bloß verglichene Kette gar nichts belegt: Wer ein Glied verändert, ohne dessen Abdruck anzufassen, bliebe unsichtbar. ⚠️ **Ruhezeiten sind Teil des Entwurfs**, nicht ein Aufsatz: Neustart und Wartungsfenster dürfen keinen Alarm auslösen — ein Wächter, dem niemand mehr glaubt, ist schlimmer als keiner. ⚠️ Eine Blume darf **nichts Teures** tun (kein Modell, kein Netz) — ein eigener Test durchsucht das Modul danach; sonst wäre der minütliche Takt nicht tragbar. ⚠️ Sie ist manipulations-**sichtbar**, nicht -**sicher** — die Meldung sagt das ausdrücklich; die Stufe darüber wären signierte Commits. ⚠️ **[NEU 25.07., C2] Sie prüfen die Anmeldung mit** (`anmeldung_pruefen`): Ein Ablaufdatum lässt sich **nicht** vorhersagen — gemessen wurde, dass das Abo-Token opak ist (`sk-ant-oat…`, 108 Zeichen, kein JWT), es enthält also keines. Stattdessen zwei deterministische Prüfungen: ist eine Anmeldung **vorhanden** (nur der NAME in `/proc/<pid>/environ`, **nie der Wert** — ein eigener Test verbietet `split("=", 1)[1]`), und hat sie zuletzt **versagt** (Auth-Wortlaute im Journal der letzten Minuten). Meldet zusätzlich, wenn statt des Abo-Tokens ein **API-Schlüssel** in der Umgebung steht — das bucht Geld ab | `.venv/bin/python scripts/test_stundenblumen.py` → „Alle Stundenblumen-Tests bestanden" (12 Prüfungen); `python3 scripts/stundenblume.py --pruefen` bewertet die Kette; im 4-Uhr-Check als eigene Zeile |
| **Durchlauf-Wache** (25.07.): Stop-Hook `.claude/hooks/durchlauf-wache.sh` + `.claude/laufplan.md` + Eintrag `Stop` in `.claude/settings.json` | Verhindert, dass die Sitzung anhält, während Arbeit offen ist. ⚠️ **Der Laufplan MUSS gepflegt werden** — ein Wächter, der einen veralteten Plan liest, ist schlimmer als keiner: Er blockiert dann gegen Erledigtes. ⚠️ **Beide Notbremsen sind Teil der Funktion**, nicht Bequemlichkeit: `WARTET: ja` und der Durchlass nach zwei Blockaden — ein Wächter, der nie durchlässt, ist ein Ausfall, kein Schutz. ⚠️ Der Hook erfindet **keine** Arbeit: Fehlt der Laufplan, lässt er durch | `CLAUDE_PROJECT_DIR=$PWD bash .claude/hooks/durchlauf-wache.sh` → Exit **2** bei offenen Punkten, **0** bei `WARTET: ja`, **0** beim dritten Versuch in Folge (Notbremse) |
| **VPS-Zeitzone Europe/Berlin** | Alle Zeitangaben des Bots + Datums-Prüfung des Hooks. Liefe der Server auf UTC, wären Prompt UND Hook konsistent falsch (Adam würde 2 h Versatz sehen) | `timedatectl \| grep -q "Europe/Berlin"` (verifiziert 17.07.: CEST, +0200 ✅) |

---

## Änderungshistorie

- **2026-07-25 (2)** — Start-Wächter (B1) eingetragen. Die Zeile hält die drei
  stillen Voraussetzungen fest, die man einer Rettungsfunktion nicht ansieht:
  abgekoppelt starten, über das Postfach melden, und dass ihre Wiederbelebung
  an `Restart=always` in der systemd-Unit hängt.

- **2026-07-25** — Medien-Transportkette (H1) eingetragen. Anlass war ein echter
  Bruch: Ein Foto sprengte die 1-MB-Transportgrenze der SDK-Leitung, der Turn
  brach viermal ab, danach starb die Sitzung — für Adam sah es aus, als nehme der
  Bot keine Nachrichten mehr an. Die Zeile hält fest, was daran die eigentliche
  Abhängigkeit ist: nicht ffmpeg allein, sondern die **Kopplung** zwischen
  gesetztem Puffer und abgeleitetem Budget. Eine feste Byte-Zahl an einer der
  beiden Stellen hätte denselben Bruch nur später wiederholt.

- **2026-07-16** — Register angelegt (Adam-Auftrag „#BEZUG!-Sicherung"), Erstbefüllung
  mit allen zum Stand Phase 2 bestehenden Ketten. Verkabelung mit 8.1/8.2 in deren
  Akzeptanzkriterien vermerkt.
- **2026-07-17** — `pending.py` + `logs/pending/` als neue Kette eingetragen (5.2 Schritt 1,
  Nachrichten-Persistenz). Bezug auf die vier `_run_job`-Rückgabe-Strings markiert (stiller
  Bruch, falls umbenannt).
- **2026-07-20** — Session-Wächter (5.18) eingetragen: die Drei-Teile-Kette
  (`last_activity` → `stall_watchdog` → Start in `post_init`) und die Stellschrauben.
  Ausdrücklich vermerkt: **Lockfreiheit** ist Teil der Funktion, nicht Schlamperei, und
  wartende Permission-Anfragen sind kein Stall.
- **2026-07-20 (2)** — Voice-Eingangsschutz eingetragen. Kernpunkt der Zeile ist die
  **Reihenfolge**: Die Sicherung muss vor dem Download stehen, sonst ist sie wirkungslos —
  eine Abhängigkeit, die kein Aufruf sichtbar macht und die nur ein Test festhalten kann.
