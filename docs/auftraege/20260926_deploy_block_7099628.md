**Zweck: WEITERGABE → Engywuck (Nachprüfung zuerst) + ENTSCHEID (Adam: Deploy in drei Schritten)** · **Zu tun: Erst prüft Engywuck die Fixes nach (Teil 1). Danach führst du die drei Schritte einzeln aus, mit einem Blick in den Bot zwischen den Schritten.**

# Deploy-Block `7099628` — 26.09.2026, 16:56

Stand auf dem Server: `e658729` (Deploy vom 26.09., 13:44). Dieser Block bringt
**19 Commits**. Nach Engywucks Rat vom 26.09. in **drei getrennten Schritten**
mit drei verschiedenen Rückwegen: Code, Stimme auf dem Server, Mac-Weg.

## Teil 1 — Für Engywuck: was nachzuprüfen ist

Seine Gegenprüfung (`20260926_gegenpruefung_mick_f1.md`) galt `e658729..9fe1b93`.
Seitdem dazugekommen, jeweils mit eigener Widerlegungsprüfung durch eine frische
Sitzung:

| Commit | Inhalt | Befunde geschlossen |
|---|---|---|
| `f059d89` | **seine** Befunde 1 bis 6 (9.4-Dialog ohne Parsen, Azure-Buchung, Freizeichen 0, `**`-Paare, Einzug roh); 7 bis 11 als F-23 | 6 von 6, fünf Gegenproben rot |
| `74bbcfe` | Block 7 nach der Widerlegungsprüfung, Prüfer mit **echtem rrsync** gegen einen Wegwerf-Server | 18 + 5, fünfzehn Gegenproben rot |
| `a2dbebe` + `7099628` | 9.2 lokale Stimme, danach deren Widerlegungsprüfung (jeder Sprechweg stuft das Ganze ein) | 3 scharf + 5 mittel + 3 klein, achtzehn Gegenproben rot |

Offen und benannt: F-23 (seine 7, 9, 10, 11), `/ttsdemo` und das Ziel des
LiteLLM-Alias „local“ (F-Liste). Regressionslauf am Mac: **98/98**.

## Teil 2 — Für Adam: drei Schritte

### Schritt 0 — was der Merge mitbringt

```bash
ssh claudebot 'cd ~/claude-telegram-bot && git fetch -q origin && git log --oneline HEAD..7099628'
```

**Prüfzeile:** genau diese 19 Zeilen, von `8eb57fc` bis `7099628`, nichts anderes:
`8eb57fc` `286311d` `a2fdc0f` `35046ed` `7a83909` `9e5fc4c` `9609532` `5ddda51`
`8a63ff4` `f681eda` `de7ad4f` `ca42d07` `98fe336` `9fe1b93` `f019c6f` `74bbcfe`
`a2dbebe` `f059d89` `7099628`. Steht etwas anderes da: **nicht ziehen**, mir
die Liste schicken.

### Schritt 1 — der Code (Blöcke 2b und 7 als Code, Azure-Attrappe, Rot-Schranke, 9.2 als Code)

```bash
ssh claudebot 'cd ~/claude-telegram-bot && git merge --ff-only 7099628 && .venv/bin/pip install -q -r requirements.txt && bash scripts/regressionstest.sh > /tmp/reg.log 2>&1; echo "rc=$?"; tail -6 /tmp/reg.log'
```

```bash
ssh claudevps 'systemctl restart claude-telegram-bot && sleep 5 && systemctl is-active claude-telegram-bot'
```

**Erwartet:** `rc=0` oder `rc=77` (übersprungene Zeilen bei laufendem Dienst), dann `active`.
`pip` bringt genau zwei neue Pakete: `piper-tts` und `pathvalidate`.

**Blick in den Bot danach** (je einer genügt):
- `/status` oder `/zimmer`: **Fett erscheint als Fettdruck**, nicht als Sternchen.
- Eine Frage an Claudia mit Sprachausgabe an: Eine **lange** Antwort kommt jetzt als Text zuerst und die Stimme darunter, eine kurze wie bisher als Stimme mit Unterschrift.
- Bis Schritt 2 spricht bei roten Inhalten weiter **Katja** (wie heute). Azure bleibt aus, solange kein Schlüssel eingetragen ist.

**Rückweg:** `git reset --hard e658729` im Server-Klon, Neustart.

### Schritt 2 — die lokale Stimme auf dem Server

```bash
ssh claudebot 'cd ~/claude-telegram-bot && bash scripts/lokale_stimme_laden.sh && .venv/bin/python sprachausgabe_lokal.py --probe'
```

**Erwartet:** `Stimme abgelegt, Pruefsummen stimmen: …`, dann
`OK Lokale Stimme spricht: … (N-fach Echtzeit)` — das ist die erste Messung auf
der Server-CPU. Kein Neustart nötig; die Stimme wird beim ersten roten Satz geladen.

**Ab jetzt ist die Stimme für Rotes Pflicht:** Das Skript setzt dazu eine Marke.
Geht die Stimme später verloren, spricht Rotes nicht still in der Cloud weiter,
sondern kommt als Text, und der Tagescheck meldet es.

**Blick in den Bot danach:** Sprachausgabe an, eine Frage, deren Antwort ein
rotes Wort trägt (etwa „Wie sollte ein sicheres Passwort aussehen?“). **Erwartet:
Thorsten spricht**, durchgehend, auch in den Teilen ohne das Wort. Weil die Ampel
noch breit anschlägt, wirst du ihn öfter hören, als wirklich Rotes vorkommt.

**Rückweg:** `rm ~/.local/share/piper-stimmen/.eingerichtet ~/.local/share/piper-stimmen/de_DE-thorsten-medium.onnx*`
als claudebot — dann spricht Rotes wieder wie vor 9.2.

### Schritt 3 — der Mac-Weg (am Mac, im Terminal)

```bash
bash ~/Projects/claude-telegram-bot/scripts/mac/videoarbeiter_einrichten.sh
```

**Erwartet**, in dieser Reihenfolge: Voraussetzungen ok · zwei Schlüssel erzeugt ·
zwei Einträge beim Server · `ls ~ von rrsync abgelehnt` (zweimal) ·
`Holschluessel: schreiben abgelehnt` · `Bringschluessel: lesen abgelehnt` ·
Arbeiter am festen Ort · Probeauftrag `… zurueckgekommen, erledigt (probe)` ·
`scharf: alle 30 Minuten und beim Anmelden`. **Bricht es irgendwo ab, ist nichts
scharf** — dann die letzten Zeilen an mich.

**Blick danach:** Am nächsten Morgen steht im Tagescheck-Protokoll die Zeile
`🖥️ Mac-Weg: 0 Auftrag/Auftraege offen, letztes Lebenszeichen vor …`.

**Rückweg:** `launchctl bootout gui/$(id -u)/com.jakuna.videoarbeiter` am Mac und
die zwei `restrict,command="/usr/bin/rrsync …"`-Zeilen aus
`~/.ssh/authorized_keys` von claudebot entfernen.
