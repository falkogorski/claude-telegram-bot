> **Zweck: WEITERGABE → Engywuck** (kurze Nachprüfung) **+ ENTSCHEID** (Adam:
> eine Frage zum Mac) · **Zu tun:** Engywuck prüft `3c8c0ed` nach, danach
> Deploy. Adam: eine Nebenwirkung bestätigen (unten, eine Zeile).

# Hotfix H-1 bis H-5 — Commit `3c8c0ed`

**Stichtag:** 10.09.2026, 13:36 (aus dem Commit abgelesen) · **Geprüft gegen:**
den Ultracode-Befund vom 12:57 · **Nenner:** 5 Funde beauftragt, 5 behoben ·
H-6 bis H-8 **nicht** angefasst (nicht beauftragt) · 6 Gegenproben ·
Regressionslauf **80/80**.

## H-1 — der offene Weg, und er war seit gestern Abend live

Deine Messung habe ich nicht nachgestellt, sondern die **Ursache** behoben:
Ort und Name binden den Inhalt nicht. Im Repo hielt ihn die Schreibsperre 8.7,
im Workspace hielt ihn nichts — und der Kommentar bei `RECHNUNGS_SKRIPTE`
behauptete das Gegenteil.

**Die Bauform ist ein Feld, kein Wächter:** `_skript_basen` gibt jetzt Tripel
zurück — Ort, Namensmenge, **Inhaltsschutz** (`sperre` oder `git`). Wer eine
dritte Basis einträgt, muss ihren Schutz benennen; er kann nicht mehr
vergessen werden, weil er kein Kommentar mehr ist.

`_unveraendert_versioniert` fragt git zweimal: versioniert (`ls-files
--error-unmatch`) und unverändert (`status --porcelain` leer). **Fail-closed
in jede Richtung** — kein git, kein Repo, Zeitüberlauf, unerwarteter Fehler:
Dialog. Der Grund steht im Dialogtext, damit Adam sieht, *warum* gefragt wird.

**Eine Bedingung, die dein Fix voraussetzt und ich nicht messen kann:** Ist
`~/workspace/rechnungen` auf dem VPS wirklich ein git-Repo? Dein eigener
Befund vom 09.09. führte „Adams offene Entscheidung zu `git init`". Ist es
keines, geht **jeder** Rechnungslauf in den Dialog — sichere Richtung, aber
Adam merkt es sofort. **Prüfzeile für den Deploy:** `git -C
~/workspace/rechnungen status --porcelain` muss antworten, nicht *not a git
repository* sagen.

Fünf Prüfzeilen in `test_bashfreigabe.py` (jetzt 146): committet läuft frei ·
überschrieben endet im Dialog · nach dem Commit wieder frei (Gegenrichtung) ·
nicht versioniert bleibt zu · **und das Repo-Skript ist von der git-Bedingung
nicht betroffen** — sonst hätte ich die beiden Basen still gleichgeschaltet.

## H-2 — kein Netz statt besserer Muster

`netzfreier_vorspann()` startet pandoc und typst unter `unshare -rn`, mit
**echtem Probelauf** statt `which`-Treffer: Manche Umgebungen haben `unshare`
und erlauben keine Benutzer-Namensräume. Ohne Namensraum **verweigert** das
Skript (Rückgabe 6).

Die Regexe bleiben, und der Kommentar sagt jetzt wofür: **Höflichkeit, nicht
Sicherheit.** Wer ein Netzbild einbaut, erfährt es benannt, statt ein Papier
ohne Bild zu bekommen.

**Adam, die eine Frage:** Auf dem **Mac** gibt es kein `unshare` — dort setzt
`konzept_pdf.py` ab jetzt nichts mehr. Für Mac-PDFs gilt weiter der direkte
pandoc-Aufruf aus `CLAUDE.md`. Wenn du das Skript am Mac gebraucht hast, sag
es; dann braucht es einen zweiten, ebenso dichten Weg — **einen Schalter, der
den Riegel abschaltet, baue ich nicht.**

Prüfer: `unshare`-Attrappe am Rand (sonst wäre der Prüfer am Mac dauerhaft
rot und binnen einer Woche abgeschaltet), plus eine Zeile, die **ohne**
Namensraum die Verweigerung misst. Dazu eine Zeile, die deine Aussage
festhält: die Referenz-Schreibweise `![a][l]` geht an der Vorprüfung vorbei —
gemessen, nicht behauptet. **Die Wirkung selbst misst nur der VPS**, das
steht als offene Prüfzeile im Bericht, nicht als grüne Zeile im Prüfer.

## H-3, H-4, H-5 — und ein Nebenfund bei H-4

**H-3:** `red` statt `add`. Der Frist-Block wird im Prüfer aus dem Tagescheck
**geschnitten und ausgeführt**, mit Attrappen für `red`/`add`; drei Fälle
(davor, am Stichtag, danach).

**H-4:** Vorgabe `None`, im Auftrag steht `ohne Absender`, und
`rechnungen_ablegen.sh` nennt sich jetzt `mac-rechnungen`. Doku-Spiegel in
`docs/boten-postfach.md` im selben Commit.

**Der Nebenfund, und er ist der lehrreichste Teil:** Die vorhandene Prüfzeile
*„das Skript nennt seinen Absender (Befund 1)"* hat den Fehler **festgehalten**
— sie verlangte ausdrücklich, dass ein Aufruf **ohne** `--herkunft` die
**große** Grenze bekommt. Mein Fix machte sie rot. **Umgestellt statt
aufgeweicht:** ohne Angabe die strenge Grenze, mit Angabe die eigene; der
ursprüngliche Zweck (das Feld darf nicht fehlen) bleibt.

Zwei Prüfer waren also gegenläufig — einer hätte H-4 finden müssen, der andere
schrieb es fest. Das ist dieselbe Klasse wie deine Tabelle in Abschnitt P:
**ein Prüfer, der die Zusage der falschen Richtung misst.**

**H-5:** Ohne `parse_mode`, Dämpfer erst nach erfolgreichem Senden. Beide
Richtungen gemessen — mit einer Bot-Attrappe, die sich wie Telegram verhält
(Markdown mit unpaarigem `_` wird abgelehnt).

## Was ich NICHT angefasst habe

**H-6, H-7, H-8** — nicht beauftragt. H-8 (nie gegen echtes pandoc gemessen)
bleibt durch H-2 unverändert offen: Der Prüfer fährt weiterhin Attrappen, und
die Zwischendatei-Frage ist damit ungeklärt. Sie gehört auf den VPS.

## Prüfzeilen für den Deploy

1. `git -C ~/workspace/rechnungen status --porcelain` antwortet (H-1).
2. Ein Rechnungslauf geht ohne Dialog durch (H-1, Gegenrichtung).
3. `unshare -rn true` endet mit 0 (H-2) — sonst setzt das PDF-Skript nichts
   mehr, und das fiele erst beim nächsten Papier auf.
4. Ein Papier mit einem Referenzbild `![a][l]` wird gesetzt, und ein
   Horchposten auf dem VPS sieht **keinen** Verbindungsversuch (H-2, die
   eigentliche Wirkung).
