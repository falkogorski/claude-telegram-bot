> **Zweck: WEITERGABE → Mick** (Adam: nur den Abschnitt „Bevor du A3
> fährst") · **Zu tun:** zwei Zeilen im Befehlsblock A3 berichtigen, dann
> ist der Block freigegeben. **Befunde 1–4: abgenommen. Kette zu Ende.**

# Abnahme Stand `403f5d9` — Befunde 1 bis 4 gemessen, Kette geschlossen

**Stichtag:** 04.09.2026, 21:37 (`date`, Berlin) · **Geprüft:** Micks Prüfer
`test_rechnungen_ablegen.py` **echt gefahren** in meinem Klon (rsync 3.2.7
nachinstalliert), dazu `test_log_sync_quittung.py`, Zielumgebung 41/43,
`bash -n`, `py_compile` · **Nenner:** 4 Befunde abgenommen · 3 Gegenproben
je exakt die notierte Zeile · **2 Stellen in A3**, die stille Nichtwirkung
erzeugen würden · **2 eigene Falschaussagen** heute, benannt.

## Abgenommen — gemessen, nicht gelesen

```
✓ der zweite Lauf ist still (Befund 1)
✓ eine echte neue Datei wird sehr wohl gemeldet (Gegenrichtung)
✓ der Serverausgang wird geraeumt (Befund 2)
✓ Apostroph im Ordnernamen kommt vollstaendig an (Befund 3)
✓ ein boesartiger Ordnername fuehrt keinen Befehl aus (Befund 3)
```

**Drei Gegenproben, erwartete Zeile vorher hingeschrieben, `__pycache__`
gelöscht, Eingriff je mit `grep` verifiziert:**

| Schutz entfernt | Erwartet rot | Gemessen |
|---|---|---|
| `--remove-source-files` weg (Backslash erhalten) | nur Befund 2 | **genau Befund 2**, vier grün |
| Zählung zurück auf `find` (Bestand) | Befund 1 | **Befund 1 + Gegenrichtung** — dieselbe Ursache, beide Zeilen sehen sie |
| Text zurück in die Befehlszeile | beide Befund-3-Zeilen | **beide** — und *„ein Ordnername hat einen Befehl auf der Gegenseite ausgeführt"*: die Beute-Datei entstand. Micks Satz „keine Theorie" ist reproduziert. |

Der Prüfer misst **Wirkung** (angekommener Postfach-Auftrag, gelöschte
Quelldatei, Beute-Datei), nicht Schreibweise — die `ssh`-Attrappe reicht
an `sh -c` durch, rsync läuft echt, das Postfach-Skript ist das echte.
**Das ist die Bauform aus der Verhaltens-Prüfer-Regel, ohne Abstrich.**

**Eigener Fehler beim Messen, damit er nicht als Befund weiterlebt:** Meine
erste Gegenprobe A nahm mit dem Schalter den Zeilenumbruch-Backslash mit —
das Skript brach, **alle fünf** Zeilen rot. Das ist kein Messwert, das ist
ein kaputter Eingriff; erkennbar daran, dass *nicht die* Zeile rot wurde, die
es messen sollte. Zweiter Anlauf sauber. Genau der dritte Griff aus der Regel
vom 29.08.

## 🔴 Bevor du A3 fährst — zwei Zeilen, die still nichts bewirken

**① `saetze.json` landet flach, nicht in `daten/`.** Die A3-Zeile

```
rsync -az …/RECHNUNGSREGELN.md …/README.md …/daten/saetze.json claudebot:~/workspace/rechnungen/
```

legt bei mehreren Quellen ohne `-R` nur die **Basisnamen** ins Ziel:
`saetze.json` läge unter `~/workspace/rechnungen/saetze.json`, der Generator
liest aber `daten/saetze.json` — das berichtigte Etikett käme nie an. **Und
die Prüfzeile merkt es nicht:** `aufloesen("Spesen:30")` rechnet die Formel
aus `Spesen_voll`, das in der **alten** Datei genauso steht. Grün, ohne dass
etwas geschah — die Klasse *Prüfzeile misst die Darstellung, nicht die Sache*,
die du gerade selbst in die Blaupause geschrieben hast.

**② `ablage.py` fehlt im Kopierbefehl.** Fall elf — die Server-Hälfte von
Befund 3, die Zeichenmenge — liegt in `scripts/ablage.py`. A3 kopiert nur
`generate_aufstellung.py`. Damit stünde die Mac-Seite (stdin) auf dem Server
bereit und die Server-Seite (Abweisung vor der Datei) **nicht** — und niemand
merkt es, weil die Mac-Seite den Angriff allein schon abfängt. Zwei Schichten
waren die Absicht; eine wäre die Wirkung.

**Berichtigte Zeile, beide Punkte:**

```bash
cd ~/Projects/rechnungen && rsync -azR RECHNUNGSREGELN.md README.md daten/saetze.json scripts/ablage.py scripts/generate_aufstellung.py claudebot:~/workspace/rechnungen/
```

`-R` erhält die Pfade relativ zum Aufrufort; `rechnungsnummern.json` bleibt
unberührt, wie A3 zu Recht verlangt. **Zwei Prüfzeilen dazu, jede misst die
Sache:** `ssh claudebot 'grep -c "Abreisetag-Satz" ~/workspace/rechnungen/daten/saetze.json'`
muss die **neue** Zahl liefern (Mick nennt sie — der alte Wortlaut steht ja
zitiert im Hinweis, also nicht 0), und Fall elf auf dem Server: der
Ablage-Aufruf mit `L'Osteria/Bar` gibt **abgewiesen auf stderr** — Mick
schreibt die Zeile, er kennt den Funktionsnamen.

## Zwei eigene Falschaussagen heute — und die Regel dahinter

**① `grep -c "017-26"`** hätte 0 geliefert und Adam in einen Regel-8-Alarm
geschickt; der Zähler speichert `17`. **② „8,40 kommt nirgends vor"** — stand
in der Aufstellung. Beide Male dasselbe: **eine Prüfzeile oder eine
Nirgends-Aussage über eine Datei, die ich nicht gelesen hatte.** Das Projekt
liegt außerhalb des Repos; ich habe aus dem Register auf den Inhalt
geschlossen. Blaupause-Zeile, universell: *Eine Prüfzeile über eine ungelesene
Datei misst eine Vermutung.* Wer die Datei nicht sehen kann, schreibt „Mick
misst und nennt die Zeile" — nicht die Zeile selbst.

Micks Zeilen dagegen hielten alle: 72/73 statt 71/72 (eine Runde alt, richtig
nachgezogen), Neustart unnötig, fünf Minuten, Klon-Adresse, dritter Klon,
Teil B berichtigt, elf Fälle ausgeschrieben, Formel gebaut und über 017-26
gerechnet.

## Kette zu Ende

Gegenprüfung → Nachprüfung → Abnahme. **Was jetzt noch käme, ginge in die
F-Liste, nicht in eine vierte Runde** (Konvergenz-Bremse). Offen ist nichts
Scharfes; die zwei A3-Zeilen sind Doku, kein Code.

**Reihenfolge für Adam:** Mick berichtigt A3 (eine Zeile, ein Commit) → Adam
fährt Teil A vollständig: A1 (Pull, 72/73 + 1 übersprungen, kein Neustart) →
A2.1–A2.6 (Filter messen, `git rm`, Zeitgeber, Umschreiben, Klone,
Zeitgeber) → A3. **Nach A2.4 sage ich Bescheid, wenn mein Klon nachgezogen
ist** — bitte kurz „umgeschrieben" hierher, dann ziehe ich.

## Bei mir

- Klon des Log-Repos nach dem Umschreiben nachziehen, melden.
- Nach A1: Claudias erste Server-Rechnung → Dialoganteil-Messung, und ob der
  Tagescheck 9j nach Hälfte 1 wirklich still ist (Zielumgebung, nicht Klon).
- Kurs-Blick: der ungelesene Abgleichs-Beleg und meine zwei Nirgends-Aussagen
  als Stichproben.
